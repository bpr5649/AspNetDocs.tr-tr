---
uid: mvc/overview/older-versions-1/contact-manager/iteration-4-make-the-application-loosely-coupled-vb
title: 'Yineleme #4 – Uygulamayı gevşek bir şekilde birleştirilmiş yapın (VB) | Microsoft Dokümanlar'
author: rick-anderson
description: Bu dördüncü yinelemede, İletişim Yöneticisi uygulamasını korumayı ve değiştirmeyi kolaylaştırmak için çeşitli yazılım tasarım desenlerinden yararlanıyoruz. Için...
ms.author: riande
ms.date: 02/20/2009
ms.assetid: 92c70297-4430-4e4e-919a-9c2333a8d09a
msc.legacyurl: /mvc/overview/older-versions-1/contact-manager/iteration-4-make-the-application-loosely-coupled-vb
msc.type: authoredcontent
ms.openlocfilehash: 1026e307b7e498a8b1392f2907c08cdcd05a3199
ms.sourcegitcommit: 022f79dbc1350e0c6ffaa1e7e7c6e850cdabf9af
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 04/17/2020
ms.locfileid: "81542774"
---
# <a name="iteration-4--make-the-application-loosely-coupled-vb"></a><span data-ttu-id="c132e-104">4. Yineleme – Uygulamanın gevşek bir şekilde bağlanmasını sağlama (VB)</span><span class="sxs-lookup"><span data-stu-id="c132e-104">Iteration #4 – Make the application loosely coupled (VB)</span></span>

<span data-ttu-id="c132e-105">[Microsoft](https://github.com/microsoft) tarafından</span><span class="sxs-lookup"><span data-stu-id="c132e-105">by [Microsoft](https://github.com/microsoft)</span></span>

[<span data-ttu-id="c132e-106">İndirme Kodu</span><span class="sxs-lookup"><span data-stu-id="c132e-106">Download Code</span></span>](iteration-4-make-the-application-loosely-coupled-vb/_static/contactmanager_4_vb1.zip)

> <span data-ttu-id="c132e-107">Bu dördüncü yinelemede, İletişim Yöneticisi uygulamasını korumayı ve değiştirmeyi kolaylaştırmak için çeşitli yazılım tasarım desenlerinden yararlanıyoruz.</span><span class="sxs-lookup"><span data-stu-id="c132e-107">In this fourth iteration, we take advantage of several software design patterns to make it easier to maintain and modify the Contact Manager application.</span></span> <span data-ttu-id="c132e-108">Örneğin, uygulamamızı Depo deseni ve Bağımlılık Enjeksiyonu modelini kullanmak üzere yeniden düzenlemeyiz.</span><span class="sxs-lookup"><span data-stu-id="c132e-108">For example, we refactor our application to use the Repository pattern and the Dependency Injection pattern.</span></span>

## <a name="building-a-contact-management-aspnet-mvc-application-vb"></a><span data-ttu-id="c132e-109">MVC Uygulaması ASP.NET İletişim Yönetimi Oluşturma (VB)</span><span class="sxs-lookup"><span data-stu-id="c132e-109">Building a Contact Management ASP.NET MVC Application (VB)</span></span>

<span data-ttu-id="c132e-110">Öğreticiler bu dizi, biz baştan sona tüm Bir İletişim Yönetimi uygulaması oluşturmak.</span><span class="sxs-lookup"><span data-stu-id="c132e-110">In this series of tutorials, we build an entire Contact Management application from start to finish.</span></span> <span data-ttu-id="c132e-111">İletişim Yöneticisi uygulaması, kişi listesi için kişi bilgilerini (adlar, telefon numaraları ve e-posta adresleri) depolamanızı sağlar.</span><span class="sxs-lookup"><span data-stu-id="c132e-111">The Contact Manager application enables you to store contact information - names, phone numbers and email addresses - for a list of people.</span></span>

<span data-ttu-id="c132e-112">Uygulamayı birden çok yineleme üzerine oluşturuyoruz.</span><span class="sxs-lookup"><span data-stu-id="c132e-112">We build the application over multiple iterations.</span></span> <span data-ttu-id="c132e-113">Her yineleme ile, yavaş yavaş uygulama geliştirmek.</span><span class="sxs-lookup"><span data-stu-id="c132e-113">With each iteration, we gradually improve the application.</span></span> <span data-ttu-id="c132e-114">Bu çoklu yineleme yaklaşımının amacı, her değişikliğin nedenini anlamanızı sağlamaktır.</span><span class="sxs-lookup"><span data-stu-id="c132e-114">The goal of this multiple iteration approach is to enable you to understand the reason for each change.</span></span>

- <span data-ttu-id="c132e-115">Yineleme #1 - Uygulamayı oluşturun.</span><span class="sxs-lookup"><span data-stu-id="c132e-115">Iteration #1 - Create the application.</span></span> <span data-ttu-id="c132e-116">İlk yinelemede, İletişim Yöneticisi'ni mümkün olan en basit şekilde oluştururuz.</span><span class="sxs-lookup"><span data-stu-id="c132e-116">In the first iteration, we create the Contact Manager in the simplest way possible.</span></span> <span data-ttu-id="c132e-117">Temel veritabanı işlemleri için destek ekliyoruz: Oluşturma, Okuma, Güncelleme ve Sil (CRUD).</span><span class="sxs-lookup"><span data-stu-id="c132e-117">We add support for basic database operations: Create, Read, Update, and Delete (CRUD).</span></span>

- <span data-ttu-id="c132e-118">Yineleme #2 - Uygulama güzel görünmesini sağlamak.</span><span class="sxs-lookup"><span data-stu-id="c132e-118">Iteration #2 - Make the application look nice.</span></span> <span data-ttu-id="c132e-119">Bu yinelemede, varsayılan ASP.NET MVC görünüm ana sayfasını ve basamaklı stil sayfasını değiştirerek uygulamanın görünümünü iyileştiriyoruz.</span><span class="sxs-lookup"><span data-stu-id="c132e-119">In this iteration, we improve the appearance of the application by modifying the default ASP.NET MVC view master page and cascading style sheet.</span></span>

- <span data-ttu-id="c132e-120">Yineleme #3 - Form doğrulama ekleyin.</span><span class="sxs-lookup"><span data-stu-id="c132e-120">Iteration #3 - Add form validation.</span></span> <span data-ttu-id="c132e-121">Üçüncü yinelemede, temel form doğrulamasını ekleriz.</span><span class="sxs-lookup"><span data-stu-id="c132e-121">In the third iteration, we add basic form validation.</span></span> <span data-ttu-id="c132e-122">İnsanların gerekli form alanlarını tamamlamadan form göndermelerini engelliyoruz.</span><span class="sxs-lookup"><span data-stu-id="c132e-122">We prevent people from submitting a form without completing required form fields.</span></span> <span data-ttu-id="c132e-123">Ayrıca e-posta adreslerini ve telefon numaralarını da doğrularız.</span><span class="sxs-lookup"><span data-stu-id="c132e-123">We also validate email addresses and phone numbers.</span></span>

- <span data-ttu-id="c132e-124">Yineleme #4 - Uygulamayı gevşek bir şekilde biraraya getirin.</span><span class="sxs-lookup"><span data-stu-id="c132e-124">Iteration #4 - Make the application loosely coupled.</span></span> <span data-ttu-id="c132e-125">Bu dördüncü yinelemede, İletişim Yöneticisi uygulamasını korumayı ve değiştirmeyi kolaylaştırmak için çeşitli yazılım tasarım desenlerinden yararlanıyoruz.</span><span class="sxs-lookup"><span data-stu-id="c132e-125">In this fourth iteration, we take advantage of several software design patterns to make it easier to maintain and modify the Contact Manager application.</span></span> <span data-ttu-id="c132e-126">Örneğin, uygulamamızı Depo deseni ve Bağımlılık Enjeksiyonu modelini kullanmak üzere yeniden düzenlemeyiz.</span><span class="sxs-lookup"><span data-stu-id="c132e-126">For example, we refactor our application to use the Repository pattern and the Dependency Injection pattern.</span></span>

- <span data-ttu-id="c132e-127">Yineleme #5 - Birim testleri oluşturun.</span><span class="sxs-lookup"><span data-stu-id="c132e-127">Iteration #5 - Create unit tests.</span></span> <span data-ttu-id="c132e-128">Beşinci yinelemede, birim testleri ekleyerek uygulamamızın korunmasını ve değiştirilmesini kolaylaştırıyoruz.</span><span class="sxs-lookup"><span data-stu-id="c132e-128">In the fifth iteration, we make our application easier to maintain and modify by adding unit tests.</span></span> <span data-ttu-id="c132e-129">Veri modeli sınıflarımızla alay ediyoruz ve denetleyicilerimiz ve doğrulama mantığımız için birim testleri oluşturuyoruz.</span><span class="sxs-lookup"><span data-stu-id="c132e-129">We mock our data model classes and build unit tests for our controllers and validation logic.</span></span>

- <span data-ttu-id="c132e-130">Yineleme #6 - Test odaklı geliştirme yi kullanın.</span><span class="sxs-lookup"><span data-stu-id="c132e-130">Iteration #6 - Use test-driven development.</span></span> <span data-ttu-id="c132e-131">Bu altıncı yinelemede, önce birim testleri yazarak ve birim testlerine karşı kod yazarak uygulamamıza yeni işlevler ekliyoruz.</span><span class="sxs-lookup"><span data-stu-id="c132e-131">In this sixth iteration, we add new functionality to our application by writing unit tests first and writing code against the unit tests.</span></span> <span data-ttu-id="c132e-132">Bu yinelemeye kişi grupları ekliyoruz.</span><span class="sxs-lookup"><span data-stu-id="c132e-132">In this iteration, we add contact groups.</span></span>

- <span data-ttu-id="c132e-133">Yineleme #7 - Ajax işlevselliği ekleyin.</span><span class="sxs-lookup"><span data-stu-id="c132e-133">Iteration #7 - Add Ajax functionality.</span></span> <span data-ttu-id="c132e-134">Yedinci yinelemede, Ajax'a destek ekleyerek uygulamamızın duyarlılığını ve performansını artırıyoruz.</span><span class="sxs-lookup"><span data-stu-id="c132e-134">In the seventh iteration, we improve the responsiveness and performance of our application by adding support for Ajax.</span></span>

## <a name="this-iteration"></a><span data-ttu-id="c132e-135">Bu Yineleme</span><span class="sxs-lookup"><span data-stu-id="c132e-135">This Iteration</span></span>

<span data-ttu-id="c132e-136">İletişim Yöneticisi uygulamasının bu dördüncü yinelemesinde, uygulamayı daha gevşek bir şekilde biraraya getirmek için uygulamayı yeniden bir araya getirebiliyoruz.</span><span class="sxs-lookup"><span data-stu-id="c132e-136">In this fourth iteration of the Contact Manager application, we refactor the application to make the application more loosely coupled.</span></span> <span data-ttu-id="c132e-137">Bir uygulama gevşek bir şekilde birleştiğinde, kodu uygulamanın diğer bölümlerinde değiştirmenize gerek kalmadan uygulamanın bir bölümünde değiştirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c132e-137">When an application is loosely coupled, you can modify the code in one part of the application without needing to modify the code in other parts of the application.</span></span> <span data-ttu-id="c132e-138">Gevşek birleştirilmiş uygulamalar değiştirmek için daha esnektir.</span><span class="sxs-lookup"><span data-stu-id="c132e-138">Loosely coupled applications are more resilient to change.</span></span>

<span data-ttu-id="c132e-139">Şu anda, Contact Manager uygulaması tarafından kullanılan tüm veri erişim ve doğrulama mantığı denetleyici sınıflarında bulunur.</span><span class="sxs-lookup"><span data-stu-id="c132e-139">Currently, all of the data access and validation logic used by the Contact Manager application is contained in the controller classes.</span></span> <span data-ttu-id="c132e-140">Bu kötü bir fikir.</span><span class="sxs-lookup"><span data-stu-id="c132e-140">This is a bad idea.</span></span> <span data-ttu-id="c132e-141">Uygulamanızın bir bölümünü değiştirmeniz gerektiğinde, uygulamanızın başka bir bölümüne hata girme riskine girersiniz.</span><span class="sxs-lookup"><span data-stu-id="c132e-141">Whenever you need to modify one part of your application, you risk introducing bugs into another part of your application.</span></span> <span data-ttu-id="c132e-142">Örneğin, doğrulama mantığınızı değiştirirseniz, veri erişiminize veya denetleyici mantığınıza yeni hatalar girme riskine girersiniz.</span><span class="sxs-lookup"><span data-stu-id="c132e-142">For example, if you modify your validation logic, you risk introducing new bugs into your data access or controller logic.</span></span>

> [!NOTE] 
> 
> <span data-ttu-id="c132e-143">(SRP), bir sınıfın değişmesi için hiçbir zaman birden fazla nedeni olmamalıdır.</span><span class="sxs-lookup"><span data-stu-id="c132e-143">(SRP), a class should never have more than one reason to change.</span></span> <span data-ttu-id="c132e-144">Karıştırma denetleyicisi, doğrulama ve veritabanı mantığı Tek Sorumluluk İlkesi büyük bir ihlalidir.</span><span class="sxs-lookup"><span data-stu-id="c132e-144">Mixing controller, validation, and database logic is a massive violation of the Single Responsibility Principle.</span></span>

<span data-ttu-id="c132e-145">Uygulamanızı değiştirmeniz gerekebilecek çeşitli nedenler vardır.</span><span class="sxs-lookup"><span data-stu-id="c132e-145">There are several reasons that you might need to modify your application.</span></span> <span data-ttu-id="c132e-146">Uygulamanıza yeni bir özellik eklemeniz gerekebilir, uygulamanızdaki bir hatayı düzeltmeniz gerekebilir veya uygulamanızın bir özelliğinin nasıl uygulandığını değiştirmeniz gerekebilir.</span><span class="sxs-lookup"><span data-stu-id="c132e-146">You might need to add a new feature to your application, you might need to fix a bug in your application, or you might need to modify how a feature of your application is implemented.</span></span> <span data-ttu-id="c132e-147">Uygulamalar nadiren statiktir.</span><span class="sxs-lookup"><span data-stu-id="c132e-147">Applications are rarely static.</span></span> <span data-ttu-id="c132e-148">Zamanla büyüyüp mutasyona uğrarlar.</span><span class="sxs-lookup"><span data-stu-id="c132e-148">They tend to grow and mutate over time.</span></span>

<span data-ttu-id="c132e-149">Örneğin, veri erişim katmanınızı uygulama şeklinizi değiştirmeye karar verdiğinizi düşünün.</span><span class="sxs-lookup"><span data-stu-id="c132e-149">Imagine, for example, that you decide to change how you implement your data access layer.</span></span> <span data-ttu-id="c132e-150">Şu anda, İletişim Yöneticisi uygulaması veritabanına erişmek için Microsoft Entity Framework'u kullanır.</span><span class="sxs-lookup"><span data-stu-id="c132e-150">Right now, the Contact Manager application uses the Microsoft Entity Framework to access the database.</span></span> <span data-ttu-id="c132e-151">Ancak, ADO.NET veri hizmetleri veya NHibernate gibi yeni veya alternatif bir veri erişim teknolojisine geçiş yapmaya karar verebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c132e-151">However, you might decide to migrate to a new or alternative data access technology such as ADO.NET Data Services or NHibernate.</span></span> <span data-ttu-id="c132e-152">Ancak, veri erişim kodu doğrulama ve denetleyici kodundan yalıtılmış olmadığından, doğrudan veri erişimiyle ilgili olmayan diğer kodu değiştirmeden uygulamanızdaki veri erişim kodunu değiştirmenin bir yolu yoktur.</span><span class="sxs-lookup"><span data-stu-id="c132e-152">However, because the data access code is not isolated from the validation and controller code, there is no way to modify the data access code in your application without modifying other code that is not directly related to data access.</span></span>

<span data-ttu-id="c132e-153">Bir uygulama gevşek bir şekilde birleştiğinde, diğer taraftan, uygulamanın diğer bölümlerine dokunmadan uygulamanın bir bölümünde değişiklik yapabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c132e-153">When an application is loosely coupled, on the other hand, you can make changes to one part of an application without touching other parts of an application.</span></span> <span data-ttu-id="c132e-154">Örneğin, doğrulama veya denetleyici mantığınızı değiştirmeden veri erişim teknolojilerinde geçiş yapabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c132e-154">For example, you can switch data access technologies without modifying your validation or controller logic.</span></span>

<span data-ttu-id="c132e-155">Bu yinelemede, İletişim Yöneticisi uygulamamızı daha gevşek bir şekilde birleştirilmiş bir uygulamaya dönüştürmemizi sağlayan çeşitli yazılım tasarım modellerinden yararlanıyoruz.</span><span class="sxs-lookup"><span data-stu-id="c132e-155">In this iteration, we take advantage of several software design patterns that enable us to refactor our Contact Manager application into a more loosely coupled application.</span></span> <span data-ttu-id="c132e-156">Biz işimiz bittiğinde, İletişim Yöneticisi daha önce yapmadığı hiçbir şeyi yapmaz.</span><span class="sxs-lookup"><span data-stu-id="c132e-156">When we are done, the Contact Manager won t do anything that it didn't do before.</span></span> <span data-ttu-id="c132e-157">Ancak, gelecekte uygulamayı daha kolay değiştirebileceğiz.</span><span class="sxs-lookup"><span data-stu-id="c132e-157">However, we'll be able to change the application more easily in the future.</span></span>

> [!NOTE] 
> 
> <span data-ttu-id="c132e-158">Yeniden düzenleme, bir uygulamayı varolan işlevselliği kaybetmeyecek şekilde yeniden yazma işlemidir.</span><span class="sxs-lookup"><span data-stu-id="c132e-158">Refactoring is the process of rewriting an application in such a way that it does not lose any existing functionality.</span></span>

## <a name="using-the-repository-software-design-pattern"></a><span data-ttu-id="c132e-159">Depo Yazılımı Tasarım Deseni Kullanma</span><span class="sxs-lookup"><span data-stu-id="c132e-159">Using the Repository Software Design Pattern</span></span>

<span data-ttu-id="c132e-160">İlk değişikliğimiz, Depo deseni adı verilen bir yazılım tasarım deseninden yararlanmaktır.</span><span class="sxs-lookup"><span data-stu-id="c132e-160">Our first change is to take advantage of a software design pattern called the Repository pattern.</span></span> <span data-ttu-id="c132e-161">Veri erişim kodumuzu uygulamamızın geri kalanından izole etmek için Depo modelini kullanacağız.</span><span class="sxs-lookup"><span data-stu-id="c132e-161">We'll use the Repository pattern to isolate our data access code from the rest of our application.</span></span>

<span data-ttu-id="c132e-162">Depo deseni uygulamak için aşağıdaki iki adımı tamamlamamız gerekmektedir:</span><span class="sxs-lookup"><span data-stu-id="c132e-162">Implementing the Repository pattern requires us to complete the following two steps:</span></span>

1. <span data-ttu-id="c132e-163">Arayüz oluşturma</span><span class="sxs-lookup"><span data-stu-id="c132e-163">Create an interface</span></span>
2. <span data-ttu-id="c132e-164">Arabirimi uygulayan somut bir sınıf oluşturma</span><span class="sxs-lookup"><span data-stu-id="c132e-164">Create a concrete class that implements the interface</span></span>

<span data-ttu-id="c132e-165">İlk olarak, gerçekleştirmemiz gereken tüm veri erişim yöntemlerini açıklayan bir arabirim oluşturmamız gerekir.</span><span class="sxs-lookup"><span data-stu-id="c132e-165">First, we need to create an interface that describes all of the data access methods that we need to perform.</span></span> <span data-ttu-id="c132e-166">IContactManagerRepository arabirimi Listeleme 1'de bulunur.</span><span class="sxs-lookup"><span data-stu-id="c132e-166">The IContactManagerRepository interface is contained in Listing 1.</span></span> <span data-ttu-id="c132e-167">Bu arabirim beş yöntem açıklar: CreateContact(), DeleteContact(), EditContact(), GetContact ve ListContacts().</span><span class="sxs-lookup"><span data-stu-id="c132e-167">This interface describes five methods: CreateContact(), DeleteContact(), EditContact(), GetContact, and ListContacts().</span></span>

<span data-ttu-id="c132e-168">**Listeleme 1 - Modeller\IContactManagerRepository.vb**</span><span class="sxs-lookup"><span data-stu-id="c132e-168">**Listing 1 - Models\IContactManagerRepository.vb**</span></span>

[!code-vb[Main](iteration-4-make-the-application-loosely-coupled-vb/samples/sample1.vb)]

<span data-ttu-id="c132e-169">Ardından, IContactManagerRepository arabirimini uygulayan somut bir sınıf oluşturmamız gerekir.</span><span class="sxs-lookup"><span data-stu-id="c132e-169">Next, we need to create a concrete class that implements the IContactManagerRepository interface.</span></span> <span data-ttu-id="c132e-170">Veritabanına erişmek için Microsoft Entity Framework'u kullandığımıziçin EntityContactManagerRepository adında yeni bir sınıf oluştururuz.</span><span class="sxs-lookup"><span data-stu-id="c132e-170">Because we are using the Microsoft Entity Framework to access the database, we'll create a new class named EntityContactManagerRepository.</span></span> <span data-ttu-id="c132e-171">Bu sınıf Listeleme 2'de yer almaktadır.</span><span class="sxs-lookup"><span data-stu-id="c132e-171">This class is contained in Listing 2.</span></span>

<span data-ttu-id="c132e-172">**Listeleme 2 - Modeller\EntityContactManagerRepository.vb**</span><span class="sxs-lookup"><span data-stu-id="c132e-172">**Listing 2 - Models\EntityContactManagerRepository.vb**</span></span>

[!code-vb[Main](iteration-4-make-the-application-loosely-coupled-vb/samples/sample2.vb)]

<span data-ttu-id="c132e-173">EntityContactManagerRepository sınıfının IContactManagerRepository arabirimini uyguladığına dikkat edin.</span><span class="sxs-lookup"><span data-stu-id="c132e-173">Notice that the EntityContactManagerRepository class implements the IContactManagerRepository interface.</span></span> <span data-ttu-id="c132e-174">Sınıf, bu arabirim tarafından açıklanan yöntemlerin beşini de uygular.</span><span class="sxs-lookup"><span data-stu-id="c132e-174">The class implements all five of the methods described by that interface.</span></span>

<span data-ttu-id="c132e-175">Neden bir arayüzle uğraşmamız gerektiğini merak edebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c132e-175">You might wonder why we need to bother with an interface.</span></span> <span data-ttu-id="c132e-176">Neden hem bir arabirim hem de onu uygulayan bir sınıf oluşturmamız gerekiyor?</span><span class="sxs-lookup"><span data-stu-id="c132e-176">Why do we need to create both an interface and a class that implements it?</span></span>

<span data-ttu-id="c132e-177">Bir istisna dışında, uygulamamızın geri kalanı beton sınıf ile değil, arabirim ile etkileşime girecektir.</span><span class="sxs-lookup"><span data-stu-id="c132e-177">With one exception, the remainder of our application will interact with the interface and not the concrete class.</span></span> <span data-ttu-id="c132e-178">EntityContactManagerRepository sınıfının maruz kaçtığı yöntemleri çağırmak yerine, IContactManagerRepository arabiriminin maruz kaçtığı yöntemleri çağıracağız.</span><span class="sxs-lookup"><span data-stu-id="c132e-178">Instead of calling the methods exposed by the EntityContactManagerRepository class, we'll call the methods exposed by the IContactManagerRepository interface.</span></span>

<span data-ttu-id="c132e-179">Bu şekilde, uygulamamızın geri kalanını değiştirmenize gerek kalmadan arabirimi yeni bir sınıfla uygulayabiliriz.</span><span class="sxs-lookup"><span data-stu-id="c132e-179">That way, we can implement the interface with a new class without needing to modify the remainder of our application.</span></span> <span data-ttu-id="c132e-180">Örneğin, ileriki bir tarihte, IContactManagerRepository arabirimini uygulayan bir DataServicesContactManagerRepository sınıfını uygulamak isteyebiliriz.</span><span class="sxs-lookup"><span data-stu-id="c132e-180">For example, at some future date, we might want to implement an DataServicesContactManagerRepository class that implements the IContactManagerRepository interface.</span></span> <span data-ttu-id="c132e-181">DataServicesContactManagerRepository sınıfı, Microsoft Entity Framework yerine bir veritabanına erişmek için ADO.NET Veri Hizmetleri'ni kullanabilir.</span><span class="sxs-lookup"><span data-stu-id="c132e-181">The DataServicesContactManagerRepository class might use ADO.NET Data Services to access a database instead of the Microsoft Entity Framework.</span></span>

<span data-ttu-id="c132e-182">Uygulama kodumuz somut EntityContactManagerRepository sınıfı yerine IContactManagerRepository arabirimine karşı programlanmışsa, kodumuzun geri kalanından herhangi birini değiştirmeden beton sınıfları arasında geçiş yapabiliriz.</span><span class="sxs-lookup"><span data-stu-id="c132e-182">If our application code is programmed against the IContactManagerRepository interface instead of the concrete EntityContactManagerRepository class then we can switch concrete classes without modifying any of the rest of our code.</span></span> <span data-ttu-id="c132e-183">Örneğin, veri erişim veya doğrulama mantığımızı değiştirmeden EntityContactManagerRepository sınıfından DataServicesContactManagerRepository sınıfına geçiş yapabiliriz.</span><span class="sxs-lookup"><span data-stu-id="c132e-183">For example, we can switch from the EntityContactManagerRepository class to the DataServicesContactManagerRepository class without modifying our data access or validation logic.</span></span>

<span data-ttu-id="c132e-184">Somut sınıflar yerine arayüzlere (soyutlamalara) karşı programlama uygulamamızı değiştirmek için daha esnek hale getirir.</span><span class="sxs-lookup"><span data-stu-id="c132e-184">Programming against interfaces (abstractions) instead of concrete classes makes our application more resilient to change.</span></span>

> [!NOTE] 
> 
> <span data-ttu-id="c132e-185">Menü seçeneği Refactor, Extract Interface seçerek Visual Studio içinde bir beton sınıfından hızlı bir arayüz oluşturabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c132e-185">You can quickly create an interface from a concrete class within Visual Studio by selecting the menu option Refactor, Extract Interface.</span></span> <span data-ttu-id="c132e-186">Örneğin, önce EntityContactManagerRepository sınıfını oluşturabilir ve ardından IContactManagerRepository arabirimini otomatik olarak oluşturmak için Extract Arabirimi'ni kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c132e-186">For example, you can create the EntityContactManagerRepository class first and then use Extract Interface to generate the IContactManagerRepository interface automatically.</span></span>

## <a name="using-the-dependency-injection-software-design-pattern"></a><span data-ttu-id="c132e-187">Bağımlılık Enjeksiyon Yazılımı Tasarım Deseni Kullanma</span><span class="sxs-lookup"><span data-stu-id="c132e-187">Using the Dependency Injection Software Design Pattern</span></span>

<span data-ttu-id="c132e-188">Veri erişim kodumuzu ayrı bir Depo sınıfına aktardığımıza göre, bu sınıfı kullanmak için İletişim denetleyicimizi değiştirmemiz gerekir.</span><span class="sxs-lookup"><span data-stu-id="c132e-188">Now that we have migrated our data access code to a separate Repository class, we need to modify our Contact controller to use this class.</span></span> <span data-ttu-id="c132e-189">Denetleyicimizde Depo sınıfını kullanmak için Bağımlılık Enjeksiyonu adı verilen bir yazılım tasarım deseninden yararlanacağız.</span><span class="sxs-lookup"><span data-stu-id="c132e-189">We will take advantage of a software design pattern called Dependency Injection to use the Repository class in our controller.</span></span>

<span data-ttu-id="c132e-190">Değiştirilmiş Kişi denetleyicisi Listeleme 3'te bulunur.</span><span class="sxs-lookup"><span data-stu-id="c132e-190">The modified Contact controller is contained in Listing 3.</span></span>

<span data-ttu-id="c132e-191">**Listeleme 3 - Denetleyiciler\ContactController.vb**</span><span class="sxs-lookup"><span data-stu-id="c132e-191">**Listing 3 - Controllers\ContactController.vb**</span></span>

[!code-vb[Main](iteration-4-make-the-application-loosely-coupled-vb/samples/sample3.vb)]

<span data-ttu-id="c132e-192">Listeleme 3'teki Kişi denetleyicisinin iki oluşturucusu olduğuna dikkat edin.</span><span class="sxs-lookup"><span data-stu-id="c132e-192">Notice that the Contact controller in Listing 3 has two constructors.</span></span> <span data-ttu-id="c132e-193">İlk oluşturucu, iContactManagerRepository arabiriminin somut bir örneğini ikinci oluşturucuya geçirir.</span><span class="sxs-lookup"><span data-stu-id="c132e-193">The first constructor passes a concrete instance of the IContactManagerRepository interface to the second constructor.</span></span> <span data-ttu-id="c132e-194">Kontak denetleyici sınıfı *Yapıcı Bağımlılık Enjeksiyonu*kullanır.</span><span class="sxs-lookup"><span data-stu-id="c132e-194">The Contact controller class uses *Constructor Dependency Injection*.</span></span>

<span data-ttu-id="c132e-195">EntityContactManagerRepository sınıfının kullanıldığı tek yer ilk oluşturucudur.</span><span class="sxs-lookup"><span data-stu-id="c132e-195">The one and only place that the EntityContactManagerRepository class is used is in the first constructor.</span></span> <span data-ttu-id="c132e-196">Sınıfın geri kalanı, somut EntityContactManagerRepository sınıfı yerine IContactManagerRepository arabirimini kullanır.</span><span class="sxs-lookup"><span data-stu-id="c132e-196">The remainder of the class uses the IContactManagerRepository interface instead of the concrete EntityContactManagerRepository class.</span></span>

<span data-ttu-id="c132e-197">Bu, gelecekte IContactManagerRepository sınıfının uygulamalarını değiştirmeyi kolaylaştırır.</span><span class="sxs-lookup"><span data-stu-id="c132e-197">This makes it easy to switch implementations of the IContactManagerRepository class in the future.</span></span> <span data-ttu-id="c132e-198">EntityContactManagerRepository sınıfı yerine DataServicesContactRepository sınıfını kullanmak istiyorsanız, ilk oluşturucuyu değiştirmeniz.</span><span class="sxs-lookup"><span data-stu-id="c132e-198">If you want to use the DataServicesContactRepository class instead of the EntityContactManagerRepository class, just modify the first constructor.</span></span>

<span data-ttu-id="c132e-199">Constructor Bağımlılık enjeksiyonu da Contact denetleyici sınıf Çok test edilebilir hale getirir.</span><span class="sxs-lookup"><span data-stu-id="c132e-199">Constructor Dependency injection also makes the Contact controller class very testable.</span></span> <span data-ttu-id="c132e-200">Birim testlerinizde, IContactManagerRepozitory sınıfının sahte bir uygulamasını geçerek Contact denetleyicisini anında atabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c132e-200">In your unit tests, you can instantiate the Contact controller by passing a mock implementation of the IContactManagerRepository class.</span></span> <span data-ttu-id="c132e-201">Bağımlılık Enjeksiyonunun bu özelliği, Contact Manager uygulaması için birim testleri oluşturduğumuz da bir sonraki yinelemede bizim için çok önemli olacaktır.</span><span class="sxs-lookup"><span data-stu-id="c132e-201">This feature of Dependency Injection will be very important to us in the next iteration when we build unit tests for the Contact Manager application.</span></span>

> [!NOTE] 
> 
> <span data-ttu-id="c132e-202">İletişim denetleyicisi sınıfını IContactManagerRepository arabiriminin belirli bir uygulamasından tamamen ayırmak istiyorsanız, StructureMap veya Microsoft Entity Framework (MEF) gibi Bağımlılık Enjeksiyonu'nu destekleyen bir çerçeveden yararlanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c132e-202">If you want to completely decouple the Contact controller class from a particular implementation of the IContactManagerRepository interface then you can take advantage of a framework that supports Dependency Injection such as StructureMap or the Microsoft Entity Framework (MEF).</span></span> <span data-ttu-id="c132e-203">Bağımlılık Enjeksiyonu çerçevesinden yararlanarak, kodunuzda somut bir sınıfa başvurmanız gerekmez.</span><span class="sxs-lookup"><span data-stu-id="c132e-203">By taking advantage of a Dependency Injection framework, you never need to refer to a concrete class in your code.</span></span>

## <a name="creating-a-service-layer"></a><span data-ttu-id="c132e-204">Hizmet Katmanı Oluşturma</span><span class="sxs-lookup"><span data-stu-id="c132e-204">Creating a Service Layer</span></span>

<span data-ttu-id="c132e-205">Doğrulama mantığımızın, Listeleme 3'teki değiştirilmiş denetleyici sınıfındadenetleyici mantığımızla hala karışık olduğunu fark etmişsinizdir.</span><span class="sxs-lookup"><span data-stu-id="c132e-205">You might have noticed that our validation logic is still mixed up with our controller logic in the modified controller class in Listing 3.</span></span> <span data-ttu-id="c132e-206">Veri erişim mantığımızı yalıtmanın iyi bir fikir olmasıyla aynı nedenle, doğrulama mantığımızı yalıtmak iyi bir fikirdir.</span><span class="sxs-lookup"><span data-stu-id="c132e-206">For the same reason that it is a good idea to isolate our data access logic, it is a good idea to isolate our validation logic.</span></span>

<span data-ttu-id="c132e-207">Bu sorunu gidermek için ayrı bir [hizmet katmanı](http://martinfowler.com/eaaCatalog/serviceLayer.html)oluşturabiliriz.</span><span class="sxs-lookup"><span data-stu-id="c132e-207">To fix this problem, we can create a separate [service layer](http://martinfowler.com/eaaCatalog/serviceLayer.html).</span></span> <span data-ttu-id="c132e-208">Hizmet katmanı, denetleyicimiz ve depo sınıflarımız arasına ekleyebileceğimiz ayrı bir katmandır.</span><span class="sxs-lookup"><span data-stu-id="c132e-208">The service layer is a separate layer that we can insert between our controller and repository classes.</span></span> <span data-ttu-id="c132e-209">Hizmet katmanı, tüm doğrulama mantığımızı da içeren iş mantığımızı içerir.</span><span class="sxs-lookup"><span data-stu-id="c132e-209">The service layer contains our business logic including all of our validation logic.</span></span>

<span data-ttu-id="c132e-210">ContactManagerService Listeleme 4'te yer almaktadır.</span><span class="sxs-lookup"><span data-stu-id="c132e-210">The ContactManagerService is contained in Listing 4.</span></span> <span data-ttu-id="c132e-211">İletişim denetleyicisi sınıfından doğrulama mantığını içerir.</span><span class="sxs-lookup"><span data-stu-id="c132e-211">It contains the validation logic from the Contact controller class.</span></span>

<span data-ttu-id="c132e-212">**Listeleme 4 - Modeller\ContactManagerService.vb**</span><span class="sxs-lookup"><span data-stu-id="c132e-212">**Listing 4 - Models\ContactManagerService.vb**</span></span>

[!code-vb[Main](iteration-4-make-the-application-loosely-coupled-vb/samples/sample4.vb)]

<span data-ttu-id="c132e-213">ContactManagerService'in oluşturucusu için doğrulama sözlüğü gerektirdiğine dikkat edin.</span><span class="sxs-lookup"><span data-stu-id="c132e-213">Notice that the constructor for the ContactManagerService requires a ValidationDictionary.</span></span> <span data-ttu-id="c132e-214">Hizmet katmanı, bu Doğrulama Sözlüğü aracılığıyla denetleyici katmanı ile iletişim kurar.</span><span class="sxs-lookup"><span data-stu-id="c132e-214">The service layer communicates with the controller layer through this ValidationDictionary.</span></span> <span data-ttu-id="c132e-215">Dekoratör deseni tartışırken Doğrulama Sözlüğü'nu aşağıdaki bölümde ayrıntılı olarak tartışıyoruz.</span><span class="sxs-lookup"><span data-stu-id="c132e-215">We discuss the ValidationDictionary in detail in the following section when we discuss the Decorator pattern.</span></span>

<span data-ttu-id="c132e-216">Ayrıca, ContactManagerService'in IContactManagerService arabirimini uyguladığına dikkat edin.</span><span class="sxs-lookup"><span data-stu-id="c132e-216">Notice, furthermore, that the ContactManagerService implements the IContactManagerService interface.</span></span> <span data-ttu-id="c132e-217">Her zaman somut sınıflar yerine arayüzlere karşı programlamak için çaba göstermelisiniz.</span><span class="sxs-lookup"><span data-stu-id="c132e-217">You should always strive to program against interfaces instead of concrete classes.</span></span> <span data-ttu-id="c132e-218">İletişim Yöneticisi uygulamasındaki diğer sınıflar ContactManagerService sınıfıyla doğrudan etkileşime geçmez.</span><span class="sxs-lookup"><span data-stu-id="c132e-218">Other classes in the Contact Manager application do not interact with the ContactManagerService class directly.</span></span> <span data-ttu-id="c132e-219">Bunun yerine, bir istisna dışında, Contact Manager uygulamasının geri kalanı IContactManagerService arabirimine göre programlanır.</span><span class="sxs-lookup"><span data-stu-id="c132e-219">Instead, with one exception, the remainder of the Contact Manager application is programmed against the IContactManagerService interface.</span></span>

<span data-ttu-id="c132e-220">IContactManagerService arabirimi Listeleme 5'te bulunur.</span><span class="sxs-lookup"><span data-stu-id="c132e-220">The IContactManagerService interface is contained in Listing 5.</span></span>

<span data-ttu-id="c132e-221">**Listeleme 5 - Modeller\IContactManagerService.vb**</span><span class="sxs-lookup"><span data-stu-id="c132e-221">**Listing 5 - Models\IContactManagerService.vb**</span></span>

[!code-vb[Main](iteration-4-make-the-application-loosely-coupled-vb/samples/sample5.vb)]

<span data-ttu-id="c132e-222">Değiştirilmiş Kişi Denetleyicisi sınıfı Listeleme 6'da bulunur.</span><span class="sxs-lookup"><span data-stu-id="c132e-222">The modified Contact controller class is contained in Listing 6.</span></span> <span data-ttu-id="c132e-223">İletişim denetleyicisinin Artık ContactManager deposuyla etkileşimde bulunmadığına dikkat edin.</span><span class="sxs-lookup"><span data-stu-id="c132e-223">Notice that the Contact controller no longer interacts with the ContactManager repository.</span></span> <span data-ttu-id="c132e-224">Bunun yerine, İletişim denetleyicisi ContactManager hizmeti ile etkileşime geçer.</span><span class="sxs-lookup"><span data-stu-id="c132e-224">Instead, the Contact controller interacts with the ContactManager service.</span></span> <span data-ttu-id="c132e-225">Her katman mümkün olduğunca diğer katmanlardan izole edilmiştir.</span><span class="sxs-lookup"><span data-stu-id="c132e-225">Each layer is isolated as much as possible from other layers.</span></span>

<span data-ttu-id="c132e-226">**Listeleme 6 - Denetleyiciler\ContactController.vb**</span><span class="sxs-lookup"><span data-stu-id="c132e-226">**Listing 6 - Controllers\ContactController.vb**</span></span>

[!code-vb[Main](iteration-4-make-the-application-loosely-coupled-vb/samples/sample6.vb)]

<span data-ttu-id="c132e-227">Uygulamamız artık Tek Sorumluluk İlkesi (SRP) afoul çalışır.</span><span class="sxs-lookup"><span data-stu-id="c132e-227">Our application no longer runs afoul of the Single Responsibility Principle (SRP).</span></span> <span data-ttu-id="c132e-228">Listeleme 6'daki İletişim denetleyicisi, uygulama yürütme akışını denetlemek dışında her türlü sorumluluktan mahrum bırakılmıştı.</span><span class="sxs-lookup"><span data-stu-id="c132e-228">The Contact controller in Listing 6 has been stripped of every responsibility other than controlling the flow of application execution.</span></span> <span data-ttu-id="c132e-229">Tüm doğrulama mantığı Contact denetleyicisinden kaldırıldı ve hizmet katmanına itildi.</span><span class="sxs-lookup"><span data-stu-id="c132e-229">All the validation logic has been removed from the Contact controller and pushed into the service layer.</span></span> <span data-ttu-id="c132e-230">Tüm veritabanı mantığı depo katmanına itildi.</span><span class="sxs-lookup"><span data-stu-id="c132e-230">All of the database logic has been pushed into the repository layer.</span></span>

## <a name="using-the-decorator-pattern"></a><span data-ttu-id="c132e-231">Dekoratör Deseni Kullanma</span><span class="sxs-lookup"><span data-stu-id="c132e-231">Using the Decorator Pattern</span></span>

<span data-ttu-id="c132e-232">Hizmet katmanımızı kontrol örneğımızdan tamamen ayırmak istiyoruz.</span><span class="sxs-lookup"><span data-stu-id="c132e-232">We want to be able to completely decouple our service layer from our controller layer.</span></span> <span data-ttu-id="c132e-233">Prensip olarak, Hizmet katmanımızı, MVC uygulamamıza bir referans eklemeye gerek kalmadan, denetleyici katmanımızdan ayrı bir derleme yapabilmemiz gerekir.</span><span class="sxs-lookup"><span data-stu-id="c132e-233">In principle, we should be able to compile our service layer in a separate assembly from our controller layer without needing to add a reference to our MVC application.</span></span>

<span data-ttu-id="c132e-234">Ancak, hizmet katmanımızın doğrulama hatası iletilerini denetleyici katmanına geri geçirebilmesi gerekir.</span><span class="sxs-lookup"><span data-stu-id="c132e-234">However, our service layer needs to be able to pass validation error messages back to the controller layer.</span></span> <span data-ttu-id="c132e-235">Denetleyici ve hizmet katmanını bağlamadan hizmet katmanının doğrulama hatası iletilerini iletmesini nasıl sağlayabiliriz?</span><span class="sxs-lookup"><span data-stu-id="c132e-235">How can we enable the service layer to communicate validation error messages without coupling the controller and service layer?</span></span> <span data-ttu-id="c132e-236">Dekoratör deseni adında bir yazılım tasarım [deseni](http://en.wikipedia.org/wiki/Decorator_pattern)yararlanabilir.</span><span class="sxs-lookup"><span data-stu-id="c132e-236">We can take advantage of a software design pattern named the [Decorator pattern](http://en.wikipedia.org/wiki/Decorator_pattern).</span></span>

<span data-ttu-id="c132e-237">Denetleyici, doğrulama hatalarını temsil etmek için ModelState Dictionary adlı bir ModelStateDictionary kullanır.</span><span class="sxs-lookup"><span data-stu-id="c132e-237">A controller uses a ModelStateDictionary named ModelState to represent validation errors.</span></span> <span data-ttu-id="c132e-238">Bu nedenle, Denetleyici katmanından hizmet katmanına ModelState geçmek için cazip olabilir.</span><span class="sxs-lookup"><span data-stu-id="c132e-238">Therefore, you might be tempted to pass ModelState from the controller layer to the service layer.</span></span> <span data-ttu-id="c132e-239">Ancak, hizmet katmanında ModelState'i kullanmak, hizmet katmanınızı ASP.NET MVC çerçevesinin bir özelliğine bağımlı hale getirecek.</span><span class="sxs-lookup"><span data-stu-id="c132e-239">However, using ModelState in the service layer would make your service layer dependent on a feature of the ASP.NET MVC framework.</span></span> <span data-ttu-id="c132e-240">Bu kötü olabilir, çünkü bir gün, ASP.NET bir MVC uygulaması yerine WPF uygulaması ile hizmet katmanı nı kullanmak isteyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="c132e-240">This would be bad because, someday, you might want to use the service layer with a WPF application instead of an ASP.NET MVC application.</span></span> <span data-ttu-id="c132e-241">Bu durumda, ModelStateDictionary sınıfını kullanmak için ASP.NET MVC çerçevesine başvurmak istemezsinüz.</span><span class="sxs-lookup"><span data-stu-id="c132e-241">In that case, you wouldn't want to reference the ASP.NET MVC framework to use the ModelStateDictionary class.</span></span>

<span data-ttu-id="c132e-242">Dekoratör deseni, arabirimi uygulamak için varolan bir sınıfı yeni bir sınıfta kaydırmanızı sağlar.</span><span class="sxs-lookup"><span data-stu-id="c132e-242">The Decorator pattern enables you to wrap an existing class in a new class in order to implement an interface.</span></span> <span data-ttu-id="c132e-243">İletişim Yöneticisi projemiz, Listeleme 7'de yer alan ModelStateWrapper sınıfını içerir.</span><span class="sxs-lookup"><span data-stu-id="c132e-243">Our Contact Manager project includes the ModelStateWrapper class contained in Listing 7.</span></span> <span data-ttu-id="c132e-244">ModelStateWrapper sınıfı Listeleme 8'deki arabirimi uygular.</span><span class="sxs-lookup"><span data-stu-id="c132e-244">The ModelStateWrapper class implements the interface in Listing 8.</span></span>

<span data-ttu-id="c132e-245">**Listeleme 7 - Modeller\Doğrulama\ModelStateWrapper.vb**</span><span class="sxs-lookup"><span data-stu-id="c132e-245">**Listing 7 - Models\Validation\ModelStateWrapper.vb**</span></span>

[!code-vb[Main](iteration-4-make-the-application-loosely-coupled-vb/samples/sample7.vb)]

<span data-ttu-id="c132e-246">**Listeleme 8 - Modeller\Doğrulama\IValidationDictionary.vb**</span><span class="sxs-lookup"><span data-stu-id="c132e-246">**Listing 8 - Models\Validation\IValidationDictionary.vb**</span></span>

[!code-vb[Main](iteration-4-make-the-application-loosely-coupled-vb/samples/sample8.vb)]

<span data-ttu-id="c132e-247">Listeleme 5'e yakından bakarsanız, ContactManager hizmet katmanının yalnızca IValidationDictionary arabirimini kullandığını görürsünüz.</span><span class="sxs-lookup"><span data-stu-id="c132e-247">If you take a close look at Listing 5 then you'll see that the ContactManager service layer uses the IValidationDictionary interface exclusively.</span></span> <span data-ttu-id="c132e-248">ContactManager hizmeti ModelStateDictionary sınıfına bağlı değildir.</span><span class="sxs-lookup"><span data-stu-id="c132e-248">The ContactManager service is not dependent on the ModelStateDictionary class.</span></span> <span data-ttu-id="c132e-249">Contact controller ContactManager hizmetini oluşturduğunda, denetleyici ModelDurumunu şu şekilde sarar:</span><span class="sxs-lookup"><span data-stu-id="c132e-249">When the Contact controller creates the ContactManager service, the controller wraps its ModelState like this:</span></span>

[!code-vb[Main](iteration-4-make-the-application-loosely-coupled-vb/samples/sample9.vb)]

## <a name="summary"></a><span data-ttu-id="c132e-250">Özet</span><span class="sxs-lookup"><span data-stu-id="c132e-250">Summary</span></span>

<span data-ttu-id="c132e-251">Bu yinelemede, İletişim Yöneticisi uygulamasına yeni bir işlev eklemedik.</span><span class="sxs-lookup"><span data-stu-id="c132e-251">In this iteration, we did not add any new functionality to the Contact Manager application.</span></span> <span data-ttu-id="c132e-252">Bu yinelemenin amacı, kişi yöneticisi uygulamasını yeniden düzenlemekti, böylece bakımı ve değiştirilmesi daha kolay.</span><span class="sxs-lookup"><span data-stu-id="c132e-252">The goal of this iteration was to refactor the Contact Manager application so that is easier to maintain and modify.</span></span>

<span data-ttu-id="c132e-253">İlk olarak, Depo yazılım tasarım modelini uyguladık.</span><span class="sxs-lookup"><span data-stu-id="c132e-253">First, we implemented the Repository software design pattern.</span></span> <span data-ttu-id="c132e-254">Tüm veri erişim kodunu ayrı bir ContactManager deposu sınıfına geçtik.</span><span class="sxs-lookup"><span data-stu-id="c132e-254">We migrated all of the data access code to a separate ContactManager repository class.</span></span>

<span data-ttu-id="c132e-255">Doğrulama mantığımızı denetleyici mantığımızdan da izole ettik.</span><span class="sxs-lookup"><span data-stu-id="c132e-255">We also isolated our validation logic from our controller logic.</span></span> <span data-ttu-id="c132e-256">Tüm doğrulama kodumuzu içeren ayrı bir hizmet katmanı oluşturduk.</span><span class="sxs-lookup"><span data-stu-id="c132e-256">We created a separate service layer that contains all of our validation code.</span></span> <span data-ttu-id="c132e-257">Denetleyici katmanı hizmet katmanıyla etkileşime giriyor ve servis katmanı depo katmanıyla etkileşime giriyor.</span><span class="sxs-lookup"><span data-stu-id="c132e-257">The controller layer interacts with the service layer, and the service layer interacts with the repository layer.</span></span>

<span data-ttu-id="c132e-258">Hizmet katmanını oluşturduğumuzda, ModelState'i hizmet katmanımızdan ayırmak için Dekoratör deseninden yararlandık.</span><span class="sxs-lookup"><span data-stu-id="c132e-258">When we created the service layer, we took advantage of the Decorator pattern to isolate ModelState from our service layer.</span></span> <span data-ttu-id="c132e-259">Hizmet katmanımızda ModelState yerine IValidationDictionary arabirimine göre programlandık.</span><span class="sxs-lookup"><span data-stu-id="c132e-259">In our service layer, we programmed against the IValidationDictionary interface instead of ModelState.</span></span>

<span data-ttu-id="c132e-260">Son olarak, Bağımlılık Enjeksiyon deseni adlı bir yazılım tasarım deseni yararlandı.</span><span class="sxs-lookup"><span data-stu-id="c132e-260">Finally, we took advantage of a software design pattern named the Dependency Injection pattern.</span></span> <span data-ttu-id="c132e-261">Bu desen, somut sınıflar yerine arabirimlere (soyutlamalar) karşı programlamamızı sağlar.</span><span class="sxs-lookup"><span data-stu-id="c132e-261">This pattern enables us to program against interfaces (abstractions) instead of concrete classes.</span></span> <span data-ttu-id="c132e-262">Bağımlılık Enjeksiyonu tasarım deseni uygulamak da kodumuzu daha test edilebilir hale getirir.</span><span class="sxs-lookup"><span data-stu-id="c132e-262">Implementing the Dependency Injection design pattern also makes our code more testable.</span></span> <span data-ttu-id="c132e-263">Bir sonraki yinelemede, projemize birim testleri ekliyoruz.</span><span class="sxs-lookup"><span data-stu-id="c132e-263">In the next iteration, we add unit tests to our project.</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="c132e-264">[Önceki](iteration-3-add-form-validation-vb.md)
> [Sonraki](iteration-5-create-unit-tests-vb.md)</span><span class="sxs-lookup"><span data-stu-id="c132e-264">[Previous](iteration-3-add-form-validation-vb.md)
[Next](iteration-5-create-unit-tests-vb.md)</span></span>
