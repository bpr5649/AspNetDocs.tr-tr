---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
title: ASP.NET Web sayfalarına giriş-HTML form temelleri | Microsoft Docs
author: Rick-Anderson
description: Bu öğreticide, ASP.NET Web Pages (Razor) kullandığınızda bir giriş formu oluşturma ve kullanıcının girişini işleme hakkında temel bilgiler gösterilmektedir. Şimdi...
ms.author: riande
ms.date: 05/28/2015
ms.assetid: 81ed82bf-b940-44f1-b94a-555d0cb7cc98
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
msc.type: authoredcontent
ms.openlocfilehash: f57661077ec3bb13f3d4ec41b130bda4d2fb9070
ms.sourcegitcommit: c62ec20b453cee3249eb894ecd75013b57d078f0
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 10/21/2020
ms.locfileid: "92345462"
---
# <a name="introducing-aspnet-web-pages---html-form-basics"></a><span data-ttu-id="69245-104">ASP.NET Web sayfalarına giriş-HTML form temelleri</span><span class="sxs-lookup"><span data-stu-id="69245-104">Introducing ASP.NET Web Pages - HTML Form Basics</span></span>

<span data-ttu-id="69245-105"> yazan: [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="69245-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="69245-106">Bu öğreticide, ASP.NET Web Pages (Razor) kullandığınızda bir giriş formu oluşturma ve kullanıcının girişini işleme hakkında temel bilgiler gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="69245-106">This tutorial shows you the basics of how to create an input form and how to handle the user's input when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="69245-107">Artık bir veritabanınız olduğuna göre, kullanıcıların veritabanında belirli filmleri bulmasına izin vermek için form becerilerinizi kullanacaksınız.</span><span class="sxs-lookup"><span data-stu-id="69245-107">And now that you've got a database, you'll use your form skills to let users find specific movies in the database.</span></span> <span data-ttu-id="69245-108">[ASP.NET Web sayfalarını kullanarak verileri görüntülemeye giriş](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data)ile seriyi tamamlamış olduğunu varsayar.</span><span class="sxs-lookup"><span data-stu-id="69245-108">It assumes you have completed the series through [Introduction to Displaying Data Using ASP.NET Web Pages](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data).</span></span>
> 
> <span data-ttu-id="69245-109">Öğrenecekleriniz:</span><span class="sxs-lookup"><span data-stu-id="69245-109">What you'll learn:</span></span>
> 
> - <span data-ttu-id="69245-110">Standart HTML öğelerini kullanarak form oluşturma.</span><span class="sxs-lookup"><span data-stu-id="69245-110">How to create a form by using standard HTML elements.</span></span>
> - <span data-ttu-id="69245-111">Kullanıcının bir formdaki girişini okuma.</span><span class="sxs-lookup"><span data-stu-id="69245-111">How to read the user's input in a form.</span></span>
> - <span data-ttu-id="69245-112">Kullanıcının sağladığı bir arama terimini kullanarak verileri seçmeli olarak alan bir SQL sorgusu oluşturma.</span><span class="sxs-lookup"><span data-stu-id="69245-112">How to create a SQL query that selectively gets data by using a search term that the user supplies.</span></span>
> - <span data-ttu-id="69245-113">"Kullanıcının ne girdiği" konusunda "hatırlama" sayfasında alan</span><span class="sxs-lookup"><span data-stu-id="69245-113">How to have fields in the page "remember" what the user entered.</span></span>
>   
> 
> <span data-ttu-id="69245-114">Ele alınan özellikler/teknolojiler:</span><span class="sxs-lookup"><span data-stu-id="69245-114">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="69245-115">`Request` nesnesi.</span><span class="sxs-lookup"><span data-stu-id="69245-115">The `Request` object.</span></span>
> - <span data-ttu-id="69245-116">SQL `Where` yan tümcesi.</span><span class="sxs-lookup"><span data-stu-id="69245-116">The SQL `Where` clause.</span></span>

## <a name="what-youll-build"></a><span data-ttu-id="69245-117">Ne oluşturacağız?</span><span class="sxs-lookup"><span data-stu-id="69245-117">What You'll Build</span></span>

<span data-ttu-id="69245-118">Önceki öğreticide, bir veritabanı oluşturdunuz, ona veri ekledi ve sonra `WebGrid` verileri göstermek için yardımcı 'yı kullandınız.</span><span class="sxs-lookup"><span data-stu-id="69245-118">In the previous tutorial, you created a database, added data to it, and then used the `WebGrid` helper to display the data.</span></span> <span data-ttu-id="69245-119">Bu öğreticide, belirli bir tarzın filmlerini bulmanıza veya başlığında girdiğiniz herhangi bir sözcük içeren bir arama kutusu ekleyeceksiniz.</span><span class="sxs-lookup"><span data-stu-id="69245-119">In this tutorial, you'll add a search box that lets you find movies of a specific genre or whose title contains whatever word you enter.</span></span> <span data-ttu-id="69245-120">(Örneğin, tarzı "Action" olan veya başlığı "Harry" veya "Adventure" içeren tüm filmleri bulabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="69245-120">(For example, you'll be able to find all movies whose genre is "Action" or whose title contains "Harry" or "Adventure.")</span></span>

<span data-ttu-id="69245-121">Bu öğreticiyi tamamladığınızda, şöyle bir sayfanız olacaktır:</span><span class="sxs-lookup"><span data-stu-id="69245-121">When you're done with this tutorial, you'll have a page like this one:</span></span>

![Tarz ve başlık arama içeren filmler sayfası](form-basics/_static/image1.png)

<span data-ttu-id="69245-123">Sayfanın listeleme bölümü, bir kılavuzun son öğreticisiyle aynıdır &mdash; .</span><span class="sxs-lookup"><span data-stu-id="69245-123">The listing part of the page is the same as in the last tutorial &mdash; a grid.</span></span> <span data-ttu-id="69245-124">Fark, kılavuzun yalnızca aradığınız filmleri gösterecektir.</span><span class="sxs-lookup"><span data-stu-id="69245-124">The difference will be that the grid will show only the movies that you searched for.</span></span>

## <a name="about-html-forms"></a><span data-ttu-id="69245-125">HTML formları hakkında</span><span class="sxs-lookup"><span data-stu-id="69245-125">About HTML Forms</span></span>

<span data-ttu-id="69245-126">(HTML formları oluşturma ve ile arasındaki fark ile ilgili deneyiminiz varsa `GET` `POST` , bu bölümü atlayabilirsiniz.)</span><span class="sxs-lookup"><span data-stu-id="69245-126">(If you've got experience with creating HTML forms and with the difference between `GET` and `POST`, you can skip this section.)</span></span>

<span data-ttu-id="69245-127">Bir formda Kullanıcı giriş öğeleri &mdash; metin kutuları, düğmeler, radyo düğmeleri, onay kutuları, açılan listeler vb. bulunur.</span><span class="sxs-lookup"><span data-stu-id="69245-127">A form has user input elements &mdash; text boxes, buttons, radio buttons, check boxes, drop-down lists, and so on.</span></span> <span data-ttu-id="69245-128">Kullanıcılar bu denetimleri doldurur veya seçimler yapar ve ardından bir düğmeye tıklayarak formu gönderir.</span><span class="sxs-lookup"><span data-stu-id="69245-128">Users fill in these controls or make selections and then submit the form by clicking a button.</span></span>

<span data-ttu-id="69245-129">Bir formun temel HTML sözdizimi bu örneğe göre gösterilmiştir:</span><span class="sxs-lookup"><span data-stu-id="69245-129">The basic HTML syntax of a form is illustrated by this example:</span></span>

[!code-html[Main](form-basics/samples/sample1.html)]

<span data-ttu-id="69245-130">Bu biçimlendirme bir sayfada çalıştığında, bu çizimi şöyle görünen basit bir form oluşturur:</span><span class="sxs-lookup"><span data-stu-id="69245-130">When this markup runs in a page, it creates a simple form that looks like this illustration:</span></span>

![Tarayıcıda işlenmiş şekilde temel HTML formu](form-basics/_static/image2.png)

<span data-ttu-id="69245-132">`<form>`Öğesi GÖNDERILECEK HTML öğelerini barındırır.</span><span class="sxs-lookup"><span data-stu-id="69245-132">The `<form>` element encloses HTML elements to be submitted.</span></span> <span data-ttu-id="69245-133">(Oluşturmanın kolay bir hata olması, sayfaya öğe eklemek ve ardından bunları bir öğe içine koymayı unutmak olur `<form>` .</span><span class="sxs-lookup"><span data-stu-id="69245-133">(An easy mistake to make is to add elements to the page but then forget to put them inside a `<form>` element.</span></span> <span data-ttu-id="69245-134">Bu durumda hiçbir şey gönderilmez.) `method` Özniteliği, tarayıcıya Kullanıcı girişini nasıl gönderebileceklerini bildirir.</span><span class="sxs-lookup"><span data-stu-id="69245-134">In that case, nothing is submitted.) The `method` attribute tells the browser how to submit the user input.</span></span> <span data-ttu-id="69245-135">`post`Sunucuda bir güncelleştirme gerçekleştirmeniz veya `get` sunucudan yalnızca veri getirmeniz durumunda bunu olarak ayarlayın.</span><span class="sxs-lookup"><span data-stu-id="69245-135">You set this to `post` if you're performing an update on the server or to `get` if you're just fetching data from the server.</span></span>

<a id="GET,_POST,_and_HTTP_Verb_Safety"></a>

> [!TIP] 
> 
> <span data-ttu-id="69245-136">**GET, POST ve HTTP fiil güvenliği**</span><span class="sxs-lookup"><span data-stu-id="69245-136">**GET, POST, and HTTP Verb Safety**</span></span>
> 
> <span data-ttu-id="69245-137">Tarayıcı ve sunucuların bilgi alışverişi için kullandığı protokol olan HTTP, temel işlemlerinde daha basit bir işlemdir.</span><span class="sxs-lookup"><span data-stu-id="69245-137">HTTP, the protocol that browsers and servers use to exchange information, is remarkably simple in its basic operations.</span></span> <span data-ttu-id="69245-138">Tarayıcılar, sunuculara istek yapmak için yalnızca birkaç fiil kullanır.</span><span class="sxs-lookup"><span data-stu-id="69245-138">Browsers use only a few verbs to make requests to servers.</span></span> <span data-ttu-id="69245-139">Web için kod yazdığınızda, bu yüklemleri ve tarayıcının ve sunucunun bunları nasıl kullandığını anlamanız yararlı olur.</span><span class="sxs-lookup"><span data-stu-id="69245-139">When you write code for the web, it's helpful to understand these verbs and how the browser and server use them.</span></span> <span data-ttu-id="69245-140">En yaygın olarak kullanılan fiiller şunlardır:</span><span class="sxs-lookup"><span data-stu-id="69245-140">Far and away the most commonly used verbs are these:</span></span>
> 
> - <span data-ttu-id="69245-141">`GET`.</span><span class="sxs-lookup"><span data-stu-id="69245-141">`GET`.</span></span> <span data-ttu-id="69245-142">Tarayıcı, sunucudan bir şeyi getirmek için bu fiili kullanır.</span><span class="sxs-lookup"><span data-stu-id="69245-142">The browser uses this verb to fetch something from the server.</span></span> <span data-ttu-id="69245-143">Örneğin, tarayıcınıza bir URL yazdığınızda, tarayıcı `GET` istediğiniz sayfayı istemek için bir işlem gerçekleştirir.</span><span class="sxs-lookup"><span data-stu-id="69245-143">For example, when you type a URL into your browser, the browser performs a `GET` operation to request the page you want.</span></span> <span data-ttu-id="69245-144">Sayfa grafik içeriyorsa, tarayıcı `GET` görüntüleri almak için ek işlemler gerçekleştirir.</span><span class="sxs-lookup"><span data-stu-id="69245-144">If the page includes graphics, the browser performs additional `GET` operations to get the images.</span></span> <span data-ttu-id="69245-145">`GET`İşlemin sunucuya bilgi geçirmesi gerekiyorsa, bilgiler sorgu DIZESINDEKI URL 'nin bir parçası olarak geçirilir.</span><span class="sxs-lookup"><span data-stu-id="69245-145">If the `GET` operation has to pass information to the server, the information is passed as part of the URL in the query string.</span></span>
> - <span data-ttu-id="69245-146">`POST`.</span><span class="sxs-lookup"><span data-stu-id="69245-146">`POST`.</span></span> <span data-ttu-id="69245-147">Tarayıcı, `POST` sunucuda eklenecek veya değiştirilecek verileri göndermek için bir istek gönderir.</span><span class="sxs-lookup"><span data-stu-id="69245-147">The browser sends a `POST` request in order to submit data to be added or changed on the server.</span></span> <span data-ttu-id="69245-148">Örneğin, `POST` fiil bir veritabanında kayıtlar oluşturmak veya var olanları değiştirmek için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="69245-148">For example, the `POST` verb is used to create records in a database or change existing ones.</span></span> <span data-ttu-id="69245-149">Çoğu zaman, bir formu doldururken ve Gönder düğmesine tıkladığınızda tarayıcı bir `POST` işlem gerçekleştirir.</span><span class="sxs-lookup"><span data-stu-id="69245-149">Most of the time, when you fill in a form and click the submit button, the browser performs a `POST` operation.</span></span> <span data-ttu-id="69245-150">Bir `POST` işlemde, sunucuya geçirilmekte olan veriler sayfanın gövdesinde bulunur.</span><span class="sxs-lookup"><span data-stu-id="69245-150">In a `POST` operation, the data being passed to the server is in the body of the page.</span></span>
> 
> <span data-ttu-id="69245-151">Bu fiiller arasındaki önemli bir ayrım, bir `GET` işlemin sunucu üzerinde herhangi bir şeyi değiştirmesi veya biraz daha soyut bir şekilde yerleştirmek için beklenmemelidir; bir `GET` işlem, sunucuda durumunda değişikliğe neden olmaz.</span><span class="sxs-lookup"><span data-stu-id="69245-151">An important distinction between these verbs is that a `GET` operation is not supposed to change anything on the server — or to put it in a slightly more abstract way, a `GET` operation does not result in a change in state on the server.</span></span> <span data-ttu-id="69245-152">`GET`Aynı kaynaklar üzerinde dilediğiniz kadar bir işlem yapabilirsiniz ve bu kaynaklar değişmez.</span><span class="sxs-lookup"><span data-stu-id="69245-152">You can perform a `GET` operation on the same resources as many times as you like, and those resources don't change.</span></span> <span data-ttu-id="69245-153">(Bir `GET` işlem genellikle "güvenli" olarak ya da teknik bir dönem kullanmak için *ıdempotent*.) Bunun aksine, bir `POST` istek işlemi gerçekleştirdiğiniz her seferinde sunucuda bir şeyi değiştirir.</span><span class="sxs-lookup"><span data-stu-id="69245-153">(A `GET` operation is often said to be "safe," or to use a technical term, is *idempotent*.) In contrast, of course, a `POST` request changes something on the server each time you perform the operation.</span></span>
> 
> <span data-ttu-id="69245-154">Bu ayrımı göstermeye yardımcı olacak iki örnek vardır.</span><span class="sxs-lookup"><span data-stu-id="69245-154">Two examples will help illustrate this distinction.</span></span> <span data-ttu-id="69245-155">Bing veya Google gibi bir altyapıyı kullanarak arama gerçekleştirdiğinizde, tek bir metin kutusundan oluşan bir formu doldurup Ara düğmesine tıklayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="69245-155">When you perform a search using an engine like Bing or Google, you fill in a form that consists of one text box, and then you click the search button.</span></span> <span data-ttu-id="69245-156">Tarayıcı, `GET` URL 'nin bir parçası olarak geçirilen kutuya girdiğiniz değerle bir işlem gerçekleştirir.</span><span class="sxs-lookup"><span data-stu-id="69245-156">The browser performs a `GET` operation, with the value you entered into the box passed as part of the URL.</span></span> <span data-ttu-id="69245-157">`GET`Bu tür bir form için bir işlem kullanmak iyidir, çünkü bir arama işlemi sunucudaki kaynakları değiştirmez, yalnızca bilgi getirir.</span><span class="sxs-lookup"><span data-stu-id="69245-157">Using a `GET` operation for this type of form is fine, because a search operation doesn't change any resources on the server, it just fetches information.</span></span>
> 
> <span data-ttu-id="69245-158">Şimdi bir şeyi çevrimiçi olarak sıralama sürecini göz önünde bulundurun.</span><span class="sxs-lookup"><span data-stu-id="69245-158">Now consider the process of ordering something online.</span></span> <span data-ttu-id="69245-159">Sipariş Ayrıntıları ' nı doldurup Gönder düğmesine tıklayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="69245-159">You fill in the order details and then click the submit button.</span></span> <span data-ttu-id="69245-160">Bu işlem bir istek olacaktır `POST` , çünkü işlem yeni bir sipariş kaydı, hesap bilgilerinde bir değişiklik ve belki de birçok başka değişiklik gibi sunucu üzerinde değişikliklere neden olur.</span><span class="sxs-lookup"><span data-stu-id="69245-160">This operation will be a `POST` request, because the operation will result in changes on the server, such as a new order record, a change in your account information, and perhaps many other changes.</span></span> <span data-ttu-id="69245-161">İşlemin aksine `GET` , isteğinizi tekrarlayükleyemezsiniz `POST` — siz isteği her yeniden yapılandırdığınızda, sunucuda yeni bir sipariş oluşturursunuz.</span><span class="sxs-lookup"><span data-stu-id="69245-161">Unlike the `GET` operation, you cannot repeat your `POST` request — if you did, each time you resubmitted the request, you'd generate a new order on the server.</span></span> <span data-ttu-id="69245-162">(Bu gibi durumlarda, Web siteleri genellikle bir Gönder düğmesine birden çok kez tıklamamanızı veya formu yanlışlıkla yeniden gönderemeyecek şekilde Gönder düğmesini devre dışı bırakacağını uyarır.)</span><span class="sxs-lookup"><span data-stu-id="69245-162">(In cases like this, websites will often warn you not to click a submit button more than once, or will disable the submit button so that you don't resubmit the form accidentally.)</span></span>
> 
> <span data-ttu-id="69245-163">Bu öğreticide, `GET` HTML formlarıyla çalışmak için hem işlem hem de bir işlem kullanacaksınız `POST` .</span><span class="sxs-lookup"><span data-stu-id="69245-163">In the course of this tutorial, you'll use both a `GET` operation and a `POST` operation to work with HTML forms.</span></span> <span data-ttu-id="69245-164">Kullandığınız fiilin her durumda uygun bir durum olduğunu açıklayacağız.</span><span class="sxs-lookup"><span data-stu-id="69245-164">We'll explain in each case why the verb you use is the appropriate one.</span></span>
> 
> <span data-ttu-id="69245-165">(HTTP fiilleri hakkında daha fazla bilgi için bkz. W3C sitesindeki [Yöntem tanımları](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) makalesi.)</span><span class="sxs-lookup"><span data-stu-id="69245-165">(To learn more about HTTP verbs, see the [Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site.)</span></span>

<span data-ttu-id="69245-166">Çoğu kullanıcı girişi öğesi HTML `<input>` öğeleridir.</span><span class="sxs-lookup"><span data-stu-id="69245-166">Most user input elements are HTML `<input>` elements.</span></span> <span data-ttu-id="69245-167">`<input type="type" name="name">,`Burada *tür* , istediğiniz kullanıcı giriş denetimi türünü gösterir.</span><span class="sxs-lookup"><span data-stu-id="69245-167">They look like `<input type="type" name="name">,` where *type* indicates the kind of user input control you want.</span></span> <span data-ttu-id="69245-168">Bu öğeler yaygın olanlardır:</span><span class="sxs-lookup"><span data-stu-id="69245-168">These elements are the common ones:</span></span>

- <span data-ttu-id="69245-169">Metin kutusu: `<input type="text">`</span><span class="sxs-lookup"><span data-stu-id="69245-169">Text box: `<input type="text">`</span></span>
- <span data-ttu-id="69245-170">Onay kutusu: `<input type="check">`</span><span class="sxs-lookup"><span data-stu-id="69245-170">Check box: `<input type="check">`</span></span>
- <span data-ttu-id="69245-171">Radyo düğmesi: `<input type="radio">`</span><span class="sxs-lookup"><span data-stu-id="69245-171">Radio button: `<input type="radio">`</span></span>
- <span data-ttu-id="69245-172">Bu `<input type="button">`</span><span class="sxs-lookup"><span data-stu-id="69245-172">Button: `<input type="button">`</span></span>
- <span data-ttu-id="69245-173">Gönder düğmesi: `<input type="submit">`</span><span class="sxs-lookup"><span data-stu-id="69245-173">Submit button: `<input type="submit">`</span></span>

<span data-ttu-id="69245-174">Ayrıca, `<textarea>` öğesini kullanarak bir çoklu satır metin kutusu ve `<select>` bir açılan liste veya kaydırılabilir liste oluşturmak için öğesi de kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="69245-174">You can also use the `<textarea>` element to create a multiline text box and the `<select>` element to create a drop-down list or scrollable list.</span></span> <span data-ttu-id="69245-175">(HTML form öğeleri hakkında daha fazla bilgi için, bkz. W3Schools sitesinde [HTML Forms and Input](http://www.w3schools.com/html/html_forms.asp) .)</span><span class="sxs-lookup"><span data-stu-id="69245-175">(For more about HTML form elements, see [HTML Forms and Input](http://www.w3schools.com/html/html_forms.asp) on the W3Schools site.)</span></span>

<span data-ttu-id="69245-176">`name`Özniteliği çok önemlidir, çünkü ad daha sonra göreceğiniz gibi, daha sonra bir öğenin değerini alabilir.</span><span class="sxs-lookup"><span data-stu-id="69245-176">The `name` attribute is very important, because the name is how you'll get the value of the element later, as you'll see shortly.</span></span>

<span data-ttu-id="69245-177">İlginç olan bölüm, sayfa geliştiricisi, kullanıcının girişi ile yapılır.</span><span class="sxs-lookup"><span data-stu-id="69245-177">The interesting part is what you, the page developer, do with the user's input.</span></span> <span data-ttu-id="69245-178">Bu öğelerle ilişkili yerleşik davranış yok.</span><span class="sxs-lookup"><span data-stu-id="69245-178">There's no built-in behavior associated with these elements.</span></span> <span data-ttu-id="69245-179">Bunun yerine, kullanıcının girdiği veya seçtiği değerleri almanız ve bunlarla ilgili bir şey yapmanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="69245-179">Instead, you have to get the values that the user has entered or selected and do something with them.</span></span> <span data-ttu-id="69245-180">Bu öğreticide öğrenirsiniz.</span><span class="sxs-lookup"><span data-stu-id="69245-180">That's what you'll learn in this tutorial.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="69245-181">**HTML5 ve giriş formları**</span><span class="sxs-lookup"><span data-stu-id="69245-181">**HTML5 and Input Forms**</span></span>
> 
> <span data-ttu-id="69245-182">Bildiğiniz gibi, HTML geçiştir ve en son sürüm (HTML5), kullanıcıların bilgi girmesi için daha sezgisel yollar desteği içerir.</span><span class="sxs-lookup"><span data-stu-id="69245-182">As you might know, HTML is in transition and the latest version (HTML5) includes support for more intuitive ways for users to enter information.</span></span> <span data-ttu-id="69245-183">Örneğin, HTML5 'de, sizin (sayfa geliştiricisi), sayfanın bir tarih girmesini istediğinizi söyleyebilir.</span><span class="sxs-lookup"><span data-stu-id="69245-183">For example, in HTML5, you (the page developer) can tell the page that you want the user to enter a date.</span></span> <span data-ttu-id="69245-184">Tarayıcı, kullanıcının el ile tarih girmesini gerektirmek yerine otomatik olarak bir takvim görüntüleyebilir.</span><span class="sxs-lookup"><span data-stu-id="69245-184">The browser can then automatically display a calendar rather than requiring the user to enter a date manually.</span></span> <span data-ttu-id="69245-185">Ancak HTML5 yenidir ve henüz tüm tarayıcılarda desteklenmez.</span><span class="sxs-lookup"><span data-stu-id="69245-185">However, HTML5 is new and is not supported in all browsers yet.</span></span>
> 
> <span data-ttu-id="69245-186">ASP.NET Web sayfaları, kullanıcı tarayıcısının bulunduğu ölçüde HTML5 girişini destekler.</span><span class="sxs-lookup"><span data-stu-id="69245-186">ASP.NET Web Pages supports HTML5 input to the extent that the user's browser does.</span></span> <span data-ttu-id="69245-187">HTML5 içindeki öğe için yeni öznitelikler hakkında fikir için `<input>` , bkz. w3schools sitesinde [HTML &lt; giriş &gt; türü özniteliği](http://www.w3schools.com/html/html_form_input_types.asp) .</span><span class="sxs-lookup"><span data-stu-id="69245-187">For an idea of the new attributes for the `<input>` element in HTML5, see [HTML &lt;input&gt; type Attribute](http://www.w3schools.com/html/html_form_input_types.asp) on the W3Schools site.</span></span>

## <a name="creating-the-form"></a><span data-ttu-id="69245-188">Form Oluşturma</span><span class="sxs-lookup"><span data-stu-id="69245-188">Creating the Form</span></span>

<span data-ttu-id="69245-189">WebMatrix 'te **dosyalar** çalışma alanında, *filmler. cshtml* sayfasını açın.</span><span class="sxs-lookup"><span data-stu-id="69245-189">In WebMatrix, in the **Files** workspace, open the *Movies.cshtml* page.</span></span>

<span data-ttu-id="69245-190">Kapanış etiketinden sonra `</h1>` ve çağrının açılış etiketinden önce `<div>` `grid.GetHtml` , aşağıdaki biçimlendirmeyi ekleyin:</span><span class="sxs-lookup"><span data-stu-id="69245-190">After the closing `</h1>` tag and before the opening `<div>` tag of the `grid.GetHtml` call, add the following markup:</span></span>

[!code-html[Main](form-basics/samples/sample2.html)]

<span data-ttu-id="69245-191">Bu biçimlendirme, adlı bir metin kutusu ve Gönder düğmesi içeren bir form oluşturur `searchGenre` .</span><span class="sxs-lookup"><span data-stu-id="69245-191">This markup creates a form that has a text box named `searchGenre` and a submit button.</span></span> <span data-ttu-id="69245-192">Metin kutusu ve Gönder düğmesi, `<form>` özniteliği olarak ayarlanmış bir öğe içine alınmıştır `method` `get` .</span><span class="sxs-lookup"><span data-stu-id="69245-192">The text box and submit button are enclosed in a `<form>` element whose `method` attribute is set to `get`.</span></span> <span data-ttu-id="69245-193">(Metin kutusunu ve Gönder düğmesini bir öğesi içine yerleştirmezseniz `<form>` , düğmeye tıkladığınızda hiçbir şey gönderilmeyecektir.) `GET` Burada fiil, sunucuda herhangi bir değişiklik yapmayan bir form oluşturduğunuz için kullanılır; yalnızca bir aramaya neden olur.</span><span class="sxs-lookup"><span data-stu-id="69245-193">(Remember that if you don't put the text box and submit button inside a `<form>` element, nothing will be submitted when you click the button.) You use the `GET` verb here because you're creating a form that does not make any changes on the server — it just results in a search.</span></span> <span data-ttu-id="69245-194">(Önceki öğreticide, bir `post` yöntemi kullandınız, bu da sunucuya değişiklikleri nasıl gönderirsiniz.</span><span class="sxs-lookup"><span data-stu-id="69245-194">(In the previous tutorial, you used a `post` method, which is how you submit changes to the server.</span></span> <span data-ttu-id="69245-195">Sonraki öğreticide bir daha görürsünüz.)</span><span class="sxs-lookup"><span data-stu-id="69245-195">You'll see that in the next tutorial again.)</span></span>

<span data-ttu-id="69245-196">Sayfayı çalıştırın.</span><span class="sxs-lookup"><span data-stu-id="69245-196">Run the page.</span></span> <span data-ttu-id="69245-197">Form için herhangi bir davranış tanımlamamış olsanız da, neye benzebileceğinize bakabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="69245-197">Although you haven't defined any behavior for the form, you can see what it looks like:</span></span>

![Tarz için arama kutusu içeren filmler sayfası](form-basics/_static/image3.png)

<span data-ttu-id="69245-199">Metin kutusuna "komedi" gibi bir değer girin.</span><span class="sxs-lookup"><span data-stu-id="69245-199">Enter a value into the text box, like "Comedy."</span></span> <span data-ttu-id="69245-200">Ardından **Ara tarzı**' ne tıklayın.</span><span class="sxs-lookup"><span data-stu-id="69245-200">Then click **Search Genre**.</span></span>

<span data-ttu-id="69245-201">Sayfanın URL 'sini bir yere göz atın.</span><span class="sxs-lookup"><span data-stu-id="69245-201">Take note of the URL of the page.</span></span> <span data-ttu-id="69245-202">`<form>`Öğenin `method` özniteliğini olarak ayarlarsanız `get` , GIRDIĞINIZ değer artık URL 'deki sorgu dizesinin bir parçasıdır, örneğin:</span><span class="sxs-lookup"><span data-stu-id="69245-202">Because you set the `<form>` element's `method` attribute to `get`, the value you entered is now part of the query string in the URL, like this:</span></span>

`http://localhost:45661/Movies.cshtml?searchGenre=Comedy`

## <a name="reading-form-values"></a><span data-ttu-id="69245-203">Form değerlerini okuma</span><span class="sxs-lookup"><span data-stu-id="69245-203">Reading Form Values</span></span>

<span data-ttu-id="69245-204">Sayfa, veritabanı verilerini alan ve sonuçları bir kılavuzda görüntüleyen bazı kodları zaten içeriyor.</span><span class="sxs-lookup"><span data-stu-id="69245-204">The page already contains some code that gets database data and displays the results in a grid.</span></span> <span data-ttu-id="69245-205">Artık, arama terimini içeren bir SQL sorgusunu çalıştırabilmeniz için metin kutusunun değerini okuyan bazı kodlar eklemeniz gerekir.</span><span class="sxs-lookup"><span data-stu-id="69245-205">Now you have to add some code that reads the value of the text box so you can run a SQL query that includes the search term.</span></span>

<span data-ttu-id="69245-206">Formun yöntemini olarak ayarladığınız için `get` , aşağıdaki gibi bir kod kullanarak metin kutusuna girilen değeri okuyabilirsiniz:</span><span class="sxs-lookup"><span data-stu-id="69245-206">Because you set the form's method to `get`, you can read the value that was entered into the text box by using code like the following:</span></span>

`var searchTerm = Request.QueryString["searchGenre"];`

<span data-ttu-id="69245-207">`Request.QueryString`Nesnesi ( `QueryString` `Request` nesnesinin özelliği), işlemin bir parçası olarak gönderilen öğelerin değerlerini içerir `GET` .</span><span class="sxs-lookup"><span data-stu-id="69245-207">The `Request.QueryString` object (the `QueryString` property of the `Request` object) includes the values of elements that were submitted as part of the `GET` operation.</span></span> <span data-ttu-id="69245-208">`Request.QueryString`Özelliği, formda gönderilen değerlerin bir *koleksiyonunu* (bir listesini) içerir.</span><span class="sxs-lookup"><span data-stu-id="69245-208">The `Request.QueryString` property contains a *collection* (a list) of the values that are submitted in the form.</span></span> <span data-ttu-id="69245-209">Tek bir değer almak için istediğiniz öğenin adını belirtirsiniz.</span><span class="sxs-lookup"><span data-stu-id="69245-209">To get any individual value, you specify the name of the element that you want.</span></span> <span data-ttu-id="69245-210">Bu nedenle, `name` `<input>` metin kutusunu oluşturan öğesinde () bir özniteliğe sahip olmanız gerekir `searchTerm` .</span><span class="sxs-lookup"><span data-stu-id="69245-210">That's why you have to have a `name` attribute on the `<input>` element (`searchTerm`) that creates the text box.</span></span> <span data-ttu-id="69245-211">(Nesne hakkında daha fazla bilgi için, `Request` [kenar çubuğuna](#BKMK_TheRequestObject) daha sonra bakın.)</span><span class="sxs-lookup"><span data-stu-id="69245-211">(For more about the `Request` object, see the [sidebar](#BKMK_TheRequestObject) later.)</span></span>

<span data-ttu-id="69245-212">Metin kutusunun değerini okumak yeterince basittir.</span><span class="sxs-lookup"><span data-stu-id="69245-212">It's simple enough to read the value of the text box.</span></span> <span data-ttu-id="69245-213">Ancak Kullanıcı, metin kutusuna hiç bir şey girmediyse ancak **Ara** ' yı tıklamadığında, arama yapmak için hiçbir şey olmadığından, tıklamayı yoksayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="69245-213">But if the user didn't enter anything at all in the text box but clicked **Search** anyway, you can ignore that click, since there's nothing to search.</span></span>

<span data-ttu-id="69245-214">Aşağıdaki kod, bu koşulların nasıl uygulanacağını gösteren bir örnektir.</span><span class="sxs-lookup"><span data-stu-id="69245-214">The following code is an example that shows how to implement these conditions.</span></span> <span data-ttu-id="69245-215">(Bu kodu henüz eklemeniz gerekmez; bunu bir süre sonra yapmanız gerekir.)</span><span class="sxs-lookup"><span data-stu-id="69245-215">(You don't have to add this code yet; you'll do that in a moment.)</span></span>

[!code-csharp[Main](form-basics/samples/sample3.cs)]

<span data-ttu-id="69245-216">Test şu şekilde aşağı doğru bir şekilde kesilir:</span><span class="sxs-lookup"><span data-stu-id="69245-216">The test breaks down in this way:</span></span>

- <span data-ttu-id="69245-217">Değerini `Request.QueryString["searchGenre"]` , adlı öğesine girilen değeri değerini alır `<input>` `searchGenre` .</span><span class="sxs-lookup"><span data-stu-id="69245-217">Get the value of `Request.QueryString["searchGenre"]`, namely the value that was entered into the `<input>` element named `searchGenre`.</span></span>
- <span data-ttu-id="69245-218">Yöntemini kullanarak boş olup olmadığını öğrenin `IsEmpty` .</span><span class="sxs-lookup"><span data-stu-id="69245-218">Find out if it's empty by using the `IsEmpty` method.</span></span> <span data-ttu-id="69245-219">Bu yöntem, bir şeyin (örneğin, form öğesi) bir değer içerip içermediğini belirlemenin standart yoludur.</span><span class="sxs-lookup"><span data-stu-id="69245-219">This method is the standard way to determine whether something (for example, a form element) contains a value.</span></span> <span data-ttu-id="69245-220">Ancak aslında yalnızca boş *değil* , bu nedenle...</span><span class="sxs-lookup"><span data-stu-id="69245-220">But really, you care only if it's *not* empty, therefore ...</span></span>
- <span data-ttu-id="69245-221">`!`Testin önüne işleci ekleyin `IsEmpty` .</span><span class="sxs-lookup"><span data-stu-id="69245-221">Add the `!` operator in front of the `IsEmpty` test.</span></span> <span data-ttu-id="69245-222">( `!` İşleç MANTıKSAL değil anlamına gelir).</span><span class="sxs-lookup"><span data-stu-id="69245-222">(The `!` operator means logical NOT).</span></span>

<span data-ttu-id="69245-223">Düz Ingilizce olarak, tüm `if` koşul aşağıdaki gibi *olur: formun searchtarz öğesi boş değilse.* ..</span><span class="sxs-lookup"><span data-stu-id="69245-223">In plain English, the entire `if` condition translates into the following: *If the form's searchGenre element is not empty, then ...*</span></span>

<span data-ttu-id="69245-224">Bu blok arama terimini kullanan bir sorgu oluşturmak için aşamayı belirler.</span><span class="sxs-lookup"><span data-stu-id="69245-224">This block sets the stage for creating a query that uses the search term.</span></span> <span data-ttu-id="69245-225">Bunu bir sonraki bölümde yapacaksınız.</span><span class="sxs-lookup"><span data-stu-id="69245-225">You'll do that in the next section.</span></span>

<a id="BKMK_TheRequestObject"></a>

> [!TIP] 
> 
> <span data-ttu-id="69245-226">**Istek nesnesi**</span><span class="sxs-lookup"><span data-stu-id="69245-226">**The Request Object**</span></span>
> 
> <span data-ttu-id="69245-227">`Request`Nesnesi, bir sayfa istendiğinde veya gönderildiğinde tarayıcının uygulamanıza gönderdiği tüm bilgileri içerir.</span><span class="sxs-lookup"><span data-stu-id="69245-227">The `Request` object contains all the information that the browser sends to your application when a page is requested or submitted.</span></span> <span data-ttu-id="69245-228">Bu nesne, kullanıcının sağladığı, metin kutusu değerleri veya karşıya yüklenecek bir dosya gibi tüm bilgileri içerir.</span><span class="sxs-lookup"><span data-stu-id="69245-228">This object includes any information that the user provides, like text box values or a file to upload.</span></span> <span data-ttu-id="69245-229">Ayrıca, tanımlama bilgileri, URL sorgu dizesindeki (varsa), çalışan sayfanın dosya yolu, kullanıcının kullandığı tarayıcı türü, tarayıcıda ayarlanan dillerin listesi ve çok daha fazlası gibi ek bilgilerin tümünü de içerir.</span><span class="sxs-lookup"><span data-stu-id="69245-229">It also includes all sorts of additional information, like cookies, values in the URL query string (if any), the file path of the page that is running, the type of browser that the user is using, the list of languages that are set in the browser, and much more.</span></span>
> 
> <span data-ttu-id="69245-230">`Request`Nesne, değerlerin bir *koleksiyon* (liste).</span><span class="sxs-lookup"><span data-stu-id="69245-230">The `Request` object is a *collection* (list) of values.</span></span> <span data-ttu-id="69245-231">Adını belirterek koleksiyondan tek bir değer alırsınız:</span><span class="sxs-lookup"><span data-stu-id="69245-231">You get an individual value out of the collection by specifying its name:</span></span>
> 
> `var someValue = Request["name"];`
> 
> <span data-ttu-id="69245-232">`Request`Nesnesi aslında çeşitli alt kümeler sunar.</span><span class="sxs-lookup"><span data-stu-id="69245-232">The `Request` object actually exposes several subsets.</span></span> <span data-ttu-id="69245-233">Örneğin:</span><span class="sxs-lookup"><span data-stu-id="69245-233">For example:</span></span>
> 
> - <span data-ttu-id="69245-234">`Request.Form``<form>`istek bir istek ise, gönderilen öğe içindeki öğelerin değerlerini verir `POST` .</span><span class="sxs-lookup"><span data-stu-id="69245-234">`Request.Form` gives you values from elements inside the submitted `<form>` element if the request is a `POST` request.</span></span>
> - <span data-ttu-id="69245-235">`Request.QueryString` size yalnızca URL 'nin sorgu dizesindeki değerleri verir.</span><span class="sxs-lookup"><span data-stu-id="69245-235">`Request.QueryString` gives you just the values in the URL's query string.</span></span> <span data-ttu-id="69245-236">(Benzer bir URL 'de `http://mysite/myapp/page?searchGenre=action&page=2` , `?searchGenre=action&page=2` URL 'nin bölümü sorgu dizesidir.)</span><span class="sxs-lookup"><span data-stu-id="69245-236">(In a URL like `http://mysite/myapp/page?searchGenre=action&page=2`, the `?searchGenre=action&page=2` section of the URL is the query string.)</span></span>
> - <span data-ttu-id="69245-237">`Request.Cookies` koleksiyon, tarayıcının gönderdiği tanımlama bilgilerine erişmenizi sağlar.</span><span class="sxs-lookup"><span data-stu-id="69245-237">`Request.Cookies` collection gives you access to cookies that the browser has sent.</span></span>
> 
> <span data-ttu-id="69245-238">Gönderilen formda olduğunu bildiğiniz bir değer almak için kullanabilirsiniz `Request["name"]` .</span><span class="sxs-lookup"><span data-stu-id="69245-238">To get a value that you know is in the submitted form, you can use `Request["name"]`.</span></span> <span data-ttu-id="69245-239">Alternatif olarak, daha belirli sürümleri `Request.Form["name"]` ( `POST` istekler için) veya `Request.QueryString["name"]` (istekler için) kullanabilirsiniz `GET` .</span><span class="sxs-lookup"><span data-stu-id="69245-239">Alternatively, you can use the more specific versions `Request.Form["name"]` (for `POST` requests) or `Request.QueryString["name"]` (for `GET` requests).</span></span> <span data-ttu-id="69245-240">Kuşkusuz, *ad* alınacak öğenin adıdır.</span><span class="sxs-lookup"><span data-stu-id="69245-240">Of course, *name* is the name of the item to get.</span></span>
> 
> <span data-ttu-id="69245-241">Almak istediğiniz öğenin adı, kullanmakta olduğunuz koleksiyon içinde benzersiz olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="69245-241">The name of the item you want to get has to be unique within the collection you're using.</span></span> <span data-ttu-id="69245-242">Bu nedenle nesne, `Request` ve gibi alt kümeleri sağlar `Request.Form` `Request.QueryString` .</span><span class="sxs-lookup"><span data-stu-id="69245-242">That's why the `Request` object provides the subsets like `Request.Form` and `Request.QueryString`.</span></span> <span data-ttu-id="69245-243">Sayfanızın adlı bir form öğesi içerdiğini `userName` ve *Ayrıca* adlı bir tanımlama bilgisi içerdiğini varsayalım `userName` .</span><span class="sxs-lookup"><span data-stu-id="69245-243">Suppose that your page contains a form element named `userName` and *also* contains a cookie named `userName`.</span></span> <span data-ttu-id="69245-244">`Request["userName"]`Bu değeri alırsanız, form değerinin mi yoksa tanımlama bilgisinin mi olmasını istediğinize belirsizdir.</span><span class="sxs-lookup"><span data-stu-id="69245-244">If you get `Request["userName"]`, it's ambiguous whether you want the form value or the cookie.</span></span> <span data-ttu-id="69245-245">Ancak, `Request.Form["userName"]` veya alırsanız `Request.Cookie["userName"]` , hangi değeri alacağınız hakkında açık olursunuz.</span><span class="sxs-lookup"><span data-stu-id="69245-245">However, if you get `Request.Form["userName"]` or `Request.Cookie["userName"]`, you're being explicit about which value to get.</span></span>
> 
> <span data-ttu-id="69245-246">Belirli bir uygulama `Request` , veya gibi ilgilendiğiniz alt kümesini kullanmak iyi bir uygulamadır `Request.Form` `Request.QueryString` .</span><span class="sxs-lookup"><span data-stu-id="69245-246">It's a good practice to be specific and use the subset of `Request` that you're interested in, like `Request.Form` or `Request.QueryString`.</span></span> <span data-ttu-id="69245-247">Bu öğreticide oluşturmakta olduğunuz basit sayfalar için büyük olasılıkla herhangi bir farklılık yapmaz.</span><span class="sxs-lookup"><span data-stu-id="69245-247">For the simple pages that you're creating in this tutorial, it probably doesn't really make any difference.</span></span> <span data-ttu-id="69245-248">Ancak, daha karmaşık sayfalar oluştururken, açık sürümü kullanarak `Request.Form` veya `Request.QueryString` sayfa bir form (veya birden çok form içerdiğinde), tanımlama bilgileri, sorgu dizesi değerleri vb. içerdiğinde ortaya çıkabilecek sorunları önlemenize yardımcı olabilir.</span><span class="sxs-lookup"><span data-stu-id="69245-248">However, as you create more complex pages, using the explicit version `Request.Form` or `Request.QueryString` can help you avoid problems that can arise when the page contains a form (or multiple forms), cookies, query string values, and so on.</span></span>

## <a name="creating-a-query-by-using-a-search-term"></a><span data-ttu-id="69245-249">Arama terimi kullanarak sorgu oluşturma</span><span class="sxs-lookup"><span data-stu-id="69245-249">Creating a Query by Using a Search Term</span></span>

<span data-ttu-id="69245-250">Artık kullanıcının girdiği arama teriminin nasıl alınacağını öğrenmiş olduğunuza göre, onu kullanan bir sorgu oluşturabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="69245-250">Now that you know how to get the search term that the user entered, you can create a query that uses it.</span></span> <span data-ttu-id="69245-251">Tüm film öğelerini veritabanından almak için, bu ifadeye benzeyen bir SQL sorgusu kullandığınızı unutmayın:</span><span class="sxs-lookup"><span data-stu-id="69245-251">Remember that to get all the movie items out of the database, you're using a SQL query that looks like this statement:</span></span>

`SELECT * FROM Movies`

<span data-ttu-id="69245-252">Yalnızca belirli filmleri almak için, yan tümce içeren bir sorgu kullanmanız gerekir `Where` .</span><span class="sxs-lookup"><span data-stu-id="69245-252">To get only certain movies, you have to use a query that includes a `Where` clause.</span></span> <span data-ttu-id="69245-253">Bu yan tümce, sorgu tarafından döndürülen satırları ayarlamanıza olanak sağlar.</span><span class="sxs-lookup"><span data-stu-id="69245-253">This clause lets you set a condition on which rows are returned by the query.</span></span> <span data-ttu-id="69245-254">İşte bir örnek:</span><span class="sxs-lookup"><span data-stu-id="69245-254">Here's an example:</span></span>

`SELECT * FROM Movies WHERE Genre = 'Action'`

<span data-ttu-id="69245-255">Temel biçim `WHERE column = value` .</span><span class="sxs-lookup"><span data-stu-id="69245-255">The basic format is `WHERE column = value`.</span></span> <span data-ttu-id="69245-256">`=` `>` `<` `<>` Ne aradığınıza bağlı olarak, tıpkı (büyüktür), (küçüktür), (eşit değildir), `<=` vb. gibi farklı işleçleri kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="69245-256">You can use different operators besides just `=`, like `>` (greater than), `<` (less than), `<>` (not equal to), `<=` (less than or equal to), etc., depending on what you're looking for.</span></span>

<span data-ttu-id="69245-257">Merak ediyorsanız SQL deyimleri büyük/küçük harfe duyarlı değildir &mdash; `SELECT` `Select` (veya hatta `select` ).</span><span class="sxs-lookup"><span data-stu-id="69245-257">In case you're wondering, SQL statements are not case sensitive &mdash; `SELECT` is the same as `Select` (or even `select`).</span></span> <span data-ttu-id="69245-258">Ancak insanlar, daha kolay okunmasını sağlamak için, ve gibi bir SQL deyimindeki anahtar kelimeleri genellikle büyük harfle `SELECT` `WHERE` okur.</span><span class="sxs-lookup"><span data-stu-id="69245-258">However, people often capitalize keywords in a SQL statement, like `SELECT` and `WHERE`, to make it easier to read.</span></span>

### <a name="passing-the-search-term-as-a-parameter"></a><span data-ttu-id="69245-259">Arama terimi parametre olarak geçiliyor</span><span class="sxs-lookup"><span data-stu-id="69245-259">Passing the search term as a parameter</span></span>

<span data-ttu-id="69245-260">Belirli bir tarzı aramak yeterince kolay ( `WHERE Genre = 'Action'` ), ancak kullanıcının girdiği tarz için arama yapmak isteyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="69245-260">Searching for a specific genre is easy enough (`WHERE Genre = 'Action'`), but you want to be able to search for any genre that the user enters.</span></span> <span data-ttu-id="69245-261">Bunu yapmak için, Aranacak değer için bir yer tutucu içeren bir SQL sorgusu oluşturun.</span><span class="sxs-lookup"><span data-stu-id="69245-261">To do that, you create as SQL query that includes a placeholder for the value to search.</span></span> <span data-ttu-id="69245-262">Bu komut şöyle görünür:</span><span class="sxs-lookup"><span data-stu-id="69245-262">It will look like this command:</span></span>

`SELECT * FROM Movies WHERE Genre = @0`

<span data-ttu-id="69245-263">Yer tutucu, `@` ardından sıfır karakter olur.</span><span class="sxs-lookup"><span data-stu-id="69245-263">The placeholder is the `@` character followed by zero.</span></span> <span data-ttu-id="69245-264">Tahmin edebildiği gibi, bir sorgu birden fazla yer tutucu içerebilir ve adı,, `@0` `@1` `@2` vb. olarak adlandırılır.</span><span class="sxs-lookup"><span data-stu-id="69245-264">As you might guess, a query can contain multiple placeholders, and they'd be named `@0`, `@1`, `@2`, etc.</span></span>

<span data-ttu-id="69245-265">Sorguyu ayarlamak ve bunu gerçekten değeri geçirmek için, aşağıdaki gibi bir kod kullanın:</span><span class="sxs-lookup"><span data-stu-id="69245-265">To set up the query and actually pass it the value, you use the code like the following:</span></span>

[!code-sql[Main](form-basics/samples/sample4.sql)]

<span data-ttu-id="69245-266">Bu kod, kılavuzda verileri göstermek için yapmış olduğunuz verilere benzer.</span><span class="sxs-lookup"><span data-stu-id="69245-266">This code is similar to what you've already done to display data in the grid.</span></span> <span data-ttu-id="69245-267">Tek farklar şunlardır:</span><span class="sxs-lookup"><span data-stu-id="69245-267">The only differences are:</span></span>

- <span data-ttu-id="69245-268">Sorgu bir yer tutucu ( `WHERE Genre = @0"` ) içeriyor.</span><span class="sxs-lookup"><span data-stu-id="69245-268">The query contains a placeholder (`WHERE Genre = @0"`).</span></span>
- <span data-ttu-id="69245-269">Sorgu bir değişkene ( `selectCommand` ) konur, önce sorguyu doğrudan yöntemine geçirtiniz `db.Query` .</span><span class="sxs-lookup"><span data-stu-id="69245-269">The query is put into a variable (`selectCommand`); before, you passed the query directly to the `db.Query` method.</span></span>
- <span data-ttu-id="69245-270">`db.Query`Yöntemini çağırdığınızda, hem sorguyu hem de yer tutucu için kullanılacak değeri geçitirsiniz.</span><span class="sxs-lookup"><span data-stu-id="69245-270">When you call the `db.Query` method, you pass both the query and the value to use for the placeholder.</span></span> <span data-ttu-id="69245-271">(Sorguda birden fazla yer tutucu varsa, bunları yönteme ayrı değerler olarak geçirirsiniz.)</span><span class="sxs-lookup"><span data-stu-id="69245-271">(If the query had multiple placeholders, you'd pass them all as separate values to the method.)</span></span>

<span data-ttu-id="69245-272">Tüm bu öğeleri birlikte yerleştirirseniz, aşağıdaki kodu alırsınız:</span><span class="sxs-lookup"><span data-stu-id="69245-272">If you put all these elements together, you get the following code:</span></span>

[!code-csharp[Main](form-basics/samples/sample5.cs)]

> [!NOTE] 
> 
> <span data-ttu-id="69245-273">**Önemli!**</span><span class="sxs-lookup"><span data-stu-id="69245-273">**Important!**</span></span> <span data-ttu-id="69245-274">`@0`SQL komutuna değer geçirmek için yer tutucuları (gibi) kullanmak, güvenlik açısından *son derece önemlidir* .</span><span class="sxs-lookup"><span data-stu-id="69245-274">Using placeholders (like `@0`) to pass values to a SQL command is *extremely important* for security.</span></span> <span data-ttu-id="69245-275">Burada gördüğünüz gibi, değişken veri yertutucuları ile birlikte SQL komutları oluşturmanız gereken tek yoldur.</span><span class="sxs-lookup"><span data-stu-id="69245-275">The way you see it here, with placeholders for variable data, is the only way you should construct SQL commands.</span></span>
> 
> <span data-ttu-id="69245-276">Değişmez değer metin ve kullanıcıdan aldığınız değerleri birlikte koyarak bir SQL ifadesini hiçbir şekilde oluşturun.</span><span class="sxs-lookup"><span data-stu-id="69245-276">Never construct a SQL statement by putting together (concatenating) literal text and values you get from the user.</span></span> <span data-ttu-id="69245-277">Kullanıcı girişini bir SQL ifadesine bitiştirme, sitenizi kötü niyetli bir kullanıcının sayfanıza bir değer gönderdiği bir *SQL ekleme saldırısında* açar.</span><span class="sxs-lookup"><span data-stu-id="69245-277">Concatenating user input into a SQL statement opens your site to a *SQL injection attack* where a malicious user submits values to your page that hack your database.</span></span> <span data-ttu-id="69245-278">(MSDN Web sitesini [ekleme](https://msdn.microsoft.com/library/ms161953.aspx) başlıklı makalede daha fazla bilgi edinebilirsiniz.)</span><span class="sxs-lookup"><span data-stu-id="69245-278">(You can read more in the article [SQL Injection](https://msdn.microsoft.com/library/ms161953.aspx) the MSDN website.)</span></span>

## <a name="updating-the-movies-page-with-search-code"></a><span data-ttu-id="69245-279">Filmler sayfasını arama koduyla güncelleştirme</span><span class="sxs-lookup"><span data-stu-id="69245-279">Updating the Movies Page with Search Code</span></span>

<span data-ttu-id="69245-280">Artık *film. cshtml* dosyasındaki kodu güncelleştirebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="69245-280">Now you can update the code in the *Movies.cshtml* file.</span></span> <span data-ttu-id="69245-281">Başlamak için, sayfanın üst kısmındaki kod bloğundaki kodu şu kodla değiştirin:</span><span class="sxs-lookup"><span data-stu-id="69245-281">To begin, replace the code in the code block at the top of the page with this code:</span></span>

[!code-csharp[Main](form-basics/samples/sample6.cs)]

<span data-ttu-id="69245-282">Buradaki fark, `selectCommand` daha sonra geçirilecek olan değişkenine sorgu koymanızdır `db.Query` .</span><span class="sxs-lookup"><span data-stu-id="69245-282">The difference here is that you've put the query into the `selectCommand` variable, which you'll pass to `db.Query` later.</span></span> <span data-ttu-id="69245-283">SQL ifadesini bir değişkene koymak, aramayı gerçekleştirmek için yapmanız gereken ifadeyi değiştirmenize olanak sağlar.</span><span class="sxs-lookup"><span data-stu-id="69245-283">Putting the SQL statement into a variable lets you change the statement, which is what you'll do to perform the search.</span></span>

<span data-ttu-id="69245-284">Ayrıca, daha sonra geri yerleştirilecek bu iki satırı da kaldırmış olursunuz:</span><span class="sxs-lookup"><span data-stu-id="69245-284">You've also removed these two lines, which you'll put back in later:</span></span>

[!code-csharp[Main](form-basics/samples/sample7.cs)]

<span data-ttu-id="69245-285">Sorguyu henüz çalıştırmak istemezsiniz (yani çağrısı `db.Query` ) ve yardımcı 'yı henüz başlatmak istemezsiniz `WebGrid` .</span><span class="sxs-lookup"><span data-stu-id="69245-285">You don't want to run the query yet (that is, call `db.Query`) and you don't want to initialize the `WebGrid` helper yet either.</span></span> <span data-ttu-id="69245-286">Bu şeyleri, SQL deyimin çalıştırılacağı iletişime aldıktan sonra gerçekleştirirsiniz.</span><span class="sxs-lookup"><span data-stu-id="69245-286">You'll do those things after you've figured out which SQL statement has to run.</span></span>

<span data-ttu-id="69245-287">Bu yeniden yazan bloğundan sonra, aramayı işlemeye yönelik yeni mantığı ekleyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="69245-287">After this rewritten block, you can add the new logic for handling the search.</span></span> <span data-ttu-id="69245-288">Tamamlanan kod aşağıdaki gibi görünür.</span><span class="sxs-lookup"><span data-stu-id="69245-288">The completed code will look like the following.</span></span> <span data-ttu-id="69245-289">Sayfanızdaki kodu, bu örnekle eşleşecek şekilde güncelleştirin:</span><span class="sxs-lookup"><span data-stu-id="69245-289">Update the code in your page so it matches this example:</span></span>

[!code-cshtml[Main](form-basics/samples/sample8.cshtml)]

<span data-ttu-id="69245-290">Sayfa artık bunun gibi çalışmaktadır.</span><span class="sxs-lookup"><span data-stu-id="69245-290">The page now works like this.</span></span> <span data-ttu-id="69245-291">Sayfa her çalıştığında, kod veritabanını açar ve `selectCommand` değişken, tablodaki tüm kayıtları alan SQL ifadesine ayarlanır `Movies` .</span><span class="sxs-lookup"><span data-stu-id="69245-291">Every time the page runs, the code opens the database and the `selectCommand` variable is set to the SQL statement that gets all the records from the `Movies` table.</span></span> <span data-ttu-id="69245-292">Kod ayrıca değişkeni de başlatır `searchTerm` .</span><span class="sxs-lookup"><span data-stu-id="69245-292">The code also initializes the `searchTerm` variable.</span></span>

<span data-ttu-id="69245-293">Ancak, geçerli istek öğesi için bir değer içeriyorsa `searchGenre` , kod `selectCommand` farklı bir sorguya (yani, `Where` bir tarz aramak için yan tümcesini içeren bir) ayarlar.</span><span class="sxs-lookup"><span data-stu-id="69245-293">However, if the current request includes a value for the `searchGenre` element, the code sets `selectCommand` to a different query — namely, to one that includes the `Where` clause to search for a genre.</span></span> <span data-ttu-id="69245-294">Ayrıca `searchTerm` , arama kutusu için geçirilmiş şekilde ayarlanır (hiçbir şey olmayabilir).</span><span class="sxs-lookup"><span data-stu-id="69245-294">It also sets `searchTerm` to whatever was passed for the search box (which might be nothing).</span></span>

<span data-ttu-id="69245-295">Hangi SQL deyimden bağımsız olarak olursa olsun `selectCommand` , kod daha sonra `db.Query` sorguyu çalıştırmak için ' i çağırır `searchTerm` .</span><span class="sxs-lookup"><span data-stu-id="69245-295">Regardless of which SQL statement is in `selectCommand`, the code then calls `db.Query` to run the query, passing it whatever is in `searchTerm`.</span></span> <span data-ttu-id="69245-296">İçinde hiçbir şey yoksa `searchTerm` , bu durum büyük değildir, çünkü bu durumda değeri yine de değerine geçirmek için parametre yoktur `selectCommand` .</span><span class="sxs-lookup"><span data-stu-id="69245-296">If there's nothing in `searchTerm`, it doesn't matter, because in that case there's no parameter to pass the value to `selectCommand` anyway.</span></span>

<span data-ttu-id="69245-297">Son olarak, kod, `WebGrid` daha önce olduğu gibi sorgu sonuçlarını kullanarak yardımcı başlatır.</span><span class="sxs-lookup"><span data-stu-id="69245-297">Finally, the code initializes the `WebGrid` helper by using the query results, just like before.</span></span>

<span data-ttu-id="69245-298">SQL ifadesini ve arama terimini değişkenlere koyarak koda esneklik eklemişseniz bunu görebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="69245-298">You can see that by putting the SQL statement and the search term into variables, you've added flexibility to the code.</span></span> <span data-ttu-id="69245-299">Bu öğreticide daha sonra göreceğiniz gibi, bu temel çerçeveyi kullanabilir ve farklı arama türleri için mantık eklemeyi koruyabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="69245-299">As you'll see later in this tutorial, you can use this basic framework and keep adding logic for different types of searches.</span></span>

## <a name="testing-the-search-by-genre-feature"></a><span data-ttu-id="69245-300">Türe göre arama özelliğini test etme</span><span class="sxs-lookup"><span data-stu-id="69245-300">Testing the Search-by-Genre Feature</span></span>

<span data-ttu-id="69245-301">WebMatrix 'te, *filmler. cshtml* sayfasını çalıştırın.</span><span class="sxs-lookup"><span data-stu-id="69245-301">In WebMatrix, run the *Movies.cshtml* page.</span></span> <span data-ttu-id="69245-302">Sayfayı tarz için metin kutusuyla görürsünüz.</span><span class="sxs-lookup"><span data-stu-id="69245-302">You see the page with the text box for genre.</span></span>

<span data-ttu-id="69245-303">Test kayıtlarınız için girmiş olduğunuz bir tarz girin ve **Ara**' ya tıklayın.</span><span class="sxs-lookup"><span data-stu-id="69245-303">Enter a genre that you've entered for one of your test records, then click **Search**.</span></span> <span data-ttu-id="69245-304">Bu zaman, yalnızca bu tarz ile eşleşen filmlerle ilgili bir liste görürsünüz:</span><span class="sxs-lookup"><span data-stu-id="69245-304">This time you see a listing of just the movies that match that genre:</span></span>

![' Comedies ' tarzı aranırken filmlere sayfa listeleme](form-basics/_static/image4.png)

<span data-ttu-id="69245-306">Farklı bir tarz girin ve tekrar arama yapın.</span><span class="sxs-lookup"><span data-stu-id="69245-306">Enter a different genre and search again.</span></span> <span data-ttu-id="69245-307">Aramanın büyük/küçük harfe duyarlı olmadığını görebilmeniz için tüm küçük harfleri veya tüm büyük harfleri kullanarak tarzı girmeyi deneyin.</span><span class="sxs-lookup"><span data-stu-id="69245-307">Try entering the genre by using all lowercase or all uppercase letters so that you can see that the search is not case sensitive.</span></span>

## <a name="remembering-what-the-user-entered"></a><span data-ttu-id="69245-308">Kullanıcının girdiği "hatırlama"</span><span class="sxs-lookup"><span data-stu-id="69245-308">"Remembering" What the User Entered</span></span>

<span data-ttu-id="69245-309">Bir tarz girdikten ve **arama tarzında**tıklandıktan sonra, bu tarz için bir liste gördünüz.</span><span class="sxs-lookup"><span data-stu-id="69245-309">You might have noticed that after you entered a genre and clicked **Search Genre**, you saw a listing for that genre.</span></span> <span data-ttu-id="69245-310">Ancak, arama metin kutusu &mdash; diğer sözcüklerde boştu, ne girdiğinizi hatıraramamıştı.</span><span class="sxs-lookup"><span data-stu-id="69245-310">However, the search text box was empty &mdash; in other words, it didn't remember what you'd entered.</span></span>

<span data-ttu-id="69245-311">Bu davranışın neden oluştuğunu anlamak önemlidir.</span><span class="sxs-lookup"><span data-stu-id="69245-311">It's important to understand why this behavior occurs.</span></span> <span data-ttu-id="69245-312">Bir sayfa gönderdiğinizde, tarayıcı Web sunucusuna bir istek gönderir.</span><span class="sxs-lookup"><span data-stu-id="69245-312">When you submit a page, the browser sends a request to the web server.</span></span> <span data-ttu-id="69245-313">ASP.NET isteği aldığında, sayfanın marka-yeni bir örneğini oluşturur, kod içinde çalıştırır ve sonra sayfayı tarayıcıda yeniden oluşturur.</span><span class="sxs-lookup"><span data-stu-id="69245-313">When ASP.NET gets the request, it creates a brand-new instance of the page, runs the code in it, and then renders the page to the browser again.</span></span> <span data-ttu-id="69245-314">Aslında, sayfa yalnızca önceki bir sürümü ile çalıştıbildiğinizi bilmez.</span><span class="sxs-lookup"><span data-stu-id="69245-314">In effect, though, the page doesn't know that you were just working with a previous version of itself.</span></span> <span data-ttu-id="69245-315">Tüm BT, içinde bazı form verileri bulunan bir istek olduğunu bilir.</span><span class="sxs-lookup"><span data-stu-id="69245-315">All it knows is that it got a request that had some form data in it.</span></span>

<span data-ttu-id="69245-316">İlk kez veya gönderdikten sonra bir sayfa istediğinizde &mdash; &mdash; Yeni bir sayfa alıyorsunuz.</span><span class="sxs-lookup"><span data-stu-id="69245-316">Every time you request a page &mdash; whether for the first time or by submitting it &mdash; you're getting a new page.</span></span> <span data-ttu-id="69245-317">Web sunucusunda son isteğiniz bellek yok.</span><span class="sxs-lookup"><span data-stu-id="69245-317">The web server has no memory of your last request.</span></span> <span data-ttu-id="69245-318">, ASP.NET yapmaz ve hiçbir ikisini de yapmaz.</span><span class="sxs-lookup"><span data-stu-id="69245-318">Neither does ASP.NET, and neither does the browser.</span></span> <span data-ttu-id="69245-319">Sayfanın bu ayrı örnekleri arasındaki tek bağlantı, aralarında iletim yaptığınız herhangi bir veri olur.</span><span class="sxs-lookup"><span data-stu-id="69245-319">The only connection between these separate instances of the page is any data that you transmit between them.</span></span> <span data-ttu-id="69245-320">Örneğin, yeni sayfa örneği, bir sayfa gönderirseniz, önceki örnek tarafından gönderilen form verileri alabilir.</span><span class="sxs-lookup"><span data-stu-id="69245-320">If you submit a page, for example, the new page instance can get the form data that was sent by the earlier instance.</span></span> <span data-ttu-id="69245-321">(Sayfalar arasında veri geçirmenin diğer bir yolu tanımlama bilgilerini kullanmaktır.)</span><span class="sxs-lookup"><span data-stu-id="69245-321">(Another way to pass data between pages is to use cookies.)</span></span>

<span data-ttu-id="69245-322">Bu durumu tanımlamanın resmi bir yolu, Web sayfalarının *durum bilgisiz*olduğu söydir.</span><span class="sxs-lookup"><span data-stu-id="69245-322">A formal way to describe this situation is to say that web pages are *stateless*.</span></span> <span data-ttu-id="69245-323">Web sunucuları ve sayfaları ve sayfadaki öğeler, sayfanın önceki durumu hakkında herhangi bir bilgi bulundurmaz.</span><span class="sxs-lookup"><span data-stu-id="69245-323">Web servers and the pages themselves and the elements in the page do not maintain any information about the previous state of a page.</span></span> <span data-ttu-id="69245-324">Web bu şekilde tasarlanmıştır çünkü bireysel isteklerin bakım durumu, genellikle binlerce, hatta yüzlerce binlerce istek (saniyede) işleyen Web sunucularının kaynaklarını hızla tüketti.</span><span class="sxs-lookup"><span data-stu-id="69245-324">The web was designed this way because maintaining state for individual requests would quickly exhaust the resources of web servers, which often handle thousands, maybe even hundreds of thousands, of requests per second.</span></span>

<span data-ttu-id="69245-325">Bu nedenle metin kutusunun boş olması neden oldu.</span><span class="sxs-lookup"><span data-stu-id="69245-325">So that's why the text box was empty.</span></span> <span data-ttu-id="69245-326">Sayfayı gönderdikten sonra, ASP.NET sayfanın yeni bir örneğini oluşturdu ve kod ve biçimlendirme üzerinden çalışır.</span><span class="sxs-lookup"><span data-stu-id="69245-326">After you submitted the page, ASP.NET created a new instance of the page and ran through the code and markup.</span></span> <span data-ttu-id="69245-327">Bu kodda, metin kutusuna bir değer ASP.NET söylenecek bir şey yok.</span><span class="sxs-lookup"><span data-stu-id="69245-327">There was nothing in that code that told ASP.NET to put a value into the text box.</span></span> <span data-ttu-id="69245-328">Bu nedenle ASP.NET hiçbir şey yapmamıştı ve metin kutusu bu değerde bir değer olmadan işlenmiştir.</span><span class="sxs-lookup"><span data-stu-id="69245-328">So ASP.NET didn't do anything, and the text box was rendered without a value in it.</span></span>

<span data-ttu-id="69245-329">Bu sorunu geçici olarak yapmanın kolay bir yolu vardır.</span><span class="sxs-lookup"><span data-stu-id="69245-329">There's actually an easy way to get around this issue.</span></span> <span data-ttu-id="69245-330">Metin *kutusuna girdiğiniz tarz,* içinde olduğu kodda sizin için kullanılabilir &mdash; `Request.QueryString["searchGenre"]` .</span><span class="sxs-lookup"><span data-stu-id="69245-330">The genre that you entered into the text box *is* available to you in code &mdash; it's in `Request.QueryString["searchGenre"]`.</span></span>

<span data-ttu-id="69245-331">`value` `searchTerm` Bu örnekte olduğu gibi, özniteliğin değerini aldığından, metin kutusu için biçimlendirmeyi güncelleştirin:</span><span class="sxs-lookup"><span data-stu-id="69245-331">Update the markup for the text box so that the `value` attribute gets its value from `searchTerm`, like this example:</span></span>

[!code-html[Main](form-basics/samples/sample9.html?highlight=1)]

<span data-ttu-id="69245-332">Bu sayfada Ayrıca, `value` `searchTerm` girmiş olduğunuz tarz da içerdiği için özniteliği değişkenine de ayarlayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="69245-332">In this page, you could have also set the `value` attribute to the `searchTerm` variable, since that variable also contains the genre you entered.</span></span> <span data-ttu-id="69245-333">Ancak, `Request` `value` Bu görevi gerçekleştirmenin standart yolu aşağıda gösterildiği gibi özniteliğini ayarlamak için nesnesini kullanmaktır.</span><span class="sxs-lookup"><span data-stu-id="69245-333">But using the `Request` object to set the `value` attribute as shown here is the standard way to accomplish this task.</span></span> <span data-ttu-id="69245-334">(Bazı durumlarda bunu yapmak istediğiniz varsayılarak &mdash; , bu sayfayı alanlarda değer *olmadan* işlemek isteyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="69245-334">(Assuming you even want to do this &mdash; in some situations, you might want to render the page *without* values in the fields.</span></span> <span data-ttu-id="69245-335">Hepsi, uygulamanızla ilgili olan yeniliklere bağlıdır.)</span><span class="sxs-lookup"><span data-stu-id="69245-335">It all depends on what's going on with your app.)</span></span>

> [!NOTE]
> <span data-ttu-id="69245-336">Parolalar için kullanılan bir metin kutusunun değerini "hatırlayamıyorum".</span><span class="sxs-lookup"><span data-stu-id="69245-336">You can't "remember" the value of a text box that's used for passwords.</span></span> <span data-ttu-id="69245-337">Bu, kullanıcıların kod kullanarak bir parola alanını doldurmasına izin veren bir güvenlik deliği olacaktır.</span><span class="sxs-lookup"><span data-stu-id="69245-337">It would be a security hole to allow people to fill in a password field by using code.</span></span>

<span data-ttu-id="69245-338">Sayfayı yeniden çalıştırın, bir tarz girin ve **tarz ara**' yı tıklatın.</span><span class="sxs-lookup"><span data-stu-id="69245-338">Run the page again, enter a genre, and click **Search Genre**.</span></span> <span data-ttu-id="69245-339">Bu süre yalnızca aramanın sonuçlarını görmemiş, ancak metin kutusu son kez girdiklerinizi anımsar:</span><span class="sxs-lookup"><span data-stu-id="69245-339">This time not only do you see the results of the search, but the text box remembers what you entered last time:</span></span>

![Metin kutusunun önceki girişin ' hatırlanan ' olduğunu gösteren sayfa](form-basics/_static/image5.png)

## <a name="searching-for-any-word-in-the-title"></a><span data-ttu-id="69245-341">Başlıktaki herhangi bir sözcük aranıyor</span><span class="sxs-lookup"><span data-stu-id="69245-341">Searching for Any Word in the Title</span></span>

<span data-ttu-id="69245-342">Artık herhangi bir tarz için arama yapabilirsiniz, ancak bir başlık aramak da isteyebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="69245-342">You can now search for any genre, but you might also want to search for a title.</span></span> <span data-ttu-id="69245-343">Arama yaptığınızda bir başlık tam olarak doğru bir şekilde almak zordur. bunun yerine başlık içinde herhangi bir yerde görünen bir sözcük arayabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="69245-343">It's hard to get a title exactly right when you search, so instead you can search for a word that appears anywhere inside a title.</span></span> <span data-ttu-id="69245-344">Bunu SQL 'de yapmak için `LIKE` aşağıdaki gibi işlecini ve sözdizimini kullanın:</span><span class="sxs-lookup"><span data-stu-id="69245-344">To do that in SQL, you use the `LIKE` operator and syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Title LIKE '%adventure%'`

<span data-ttu-id="69245-345">Bu komut, başlıkları "Adventure" içeren tüm filmleri alır.</span><span class="sxs-lookup"><span data-stu-id="69245-345">This command gets all the movies whose titles contain "adventure".</span></span> <span data-ttu-id="69245-346">`LIKE`İşlecini kullandığınızda, `%` arama teriminin bir parçası olarak joker karakterini dahil edersiniz.</span><span class="sxs-lookup"><span data-stu-id="69245-346">When you use the `LIKE` operator, you include the wildcard character `%` as part of the search term.</span></span> <span data-ttu-id="69245-347">Arama `LIKE 'adventure%'` "' Adventure ' ile başlıyor" anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="69245-347">The search `LIKE 'adventure%'` means "starting with 'adventure'".</span></span> <span data-ttu-id="69245-348">(Teknik olarak, "Adventure" dizesinin ardından herhangi bir şey olduğu anlamına gelir. ") Benzer şekilde, arama terimi `LIKE '%adventure'` "' Adventure '" dizesi tarafından izlenen "" Adventure "ile biten" söylemenin bir diğer yolu anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="69245-348">(Technically, it means "The string 'adventure' followed by anything.") Similarly, the search term `LIKE '%adventure'` means "anything followed by the string 'adventure'", which is another way to say "ending with 'adventure'".</span></span>

<span data-ttu-id="69245-349">Bu nedenle arama terimi, `LIKE '%adventure%'` "Adventure" başlığında her yerde "Adventure" anlamına gelir. "</span><span class="sxs-lookup"><span data-stu-id="69245-349">The search term `LIKE '%adventure%'` therefore means "with 'adventure' anywhere in the title."</span></span> <span data-ttu-id="69245-350">(Teknik olarak, başlıktaki her şey, ' Adventure ' ve ardından herhangi bir şey). ")</span><span class="sxs-lookup"><span data-stu-id="69245-350">(Technically, "anything in the title, followed by 'adventure', followed by anything.")</span></span>

<span data-ttu-id="69245-351">Öğesinin içinde `<form>` , aşağıdaki biçimlendirmeyi, tarz aramasının sağ etiketi altına ekleyin `</div>` (kapanış öğesinden hemen önce `</form>` ):</span><span class="sxs-lookup"><span data-stu-id="69245-351">Inside the `<form>` element, add the following markup right under the closing `</div>` tag for the genre search (just before the closing `</form>` element):</span></span>

[!code-html[Main](form-basics/samples/sample10.html)]

<span data-ttu-id="69245-352">Bu aramayı işleme kodu, ara değer arama koduna benzer, ancak aramayı birleştirme yapmanız gerekir `LIKE` .</span><span class="sxs-lookup"><span data-stu-id="69245-352">The code to handle this search is similar to the code for the genre search, except that you have to assemble the `LIKE` search.</span></span> <span data-ttu-id="69245-353">Sayfanın üst kısmındaki kod bloğunun içinde, bu `if` bloğu yalnızca `if` tarz arama bloğundan sonra ekleyin:</span><span class="sxs-lookup"><span data-stu-id="69245-353">Inside the code block at the top of the page, add this `if` block just after the `if` block for the genre search:</span></span>

[!code-csharp[Main](form-basics/samples/sample11.cs)]

<span data-ttu-id="69245-354">Bu kod, daha önce gördüğünüz mantığı kullanır, ancak arama bir `LIKE` işleç kullanır ve kod, `%` arama teriminden önce ve sonra "" koyar.</span><span class="sxs-lookup"><span data-stu-id="69245-354">This code uses the same logic you saw earlier, except that the search uses a `LIKE` operator and the code puts "`%`" before and after the search term.</span></span>

<span data-ttu-id="69245-355">Sayfada başka bir aramanın nasıl daha kolay bir şekilde ekleneceğini fark edin.</span><span class="sxs-lookup"><span data-stu-id="69245-355">Notice how it was easy to add another search to the page.</span></span> <span data-ttu-id="69245-356">Yapmanız gerekdik:</span><span class="sxs-lookup"><span data-stu-id="69245-356">All you had to do was:</span></span>

- <span data-ttu-id="69245-357">`if`İlgili arama kutusunun bir değere sahip olup olmadığını görmek için test edilmiş bir blok oluşturun.</span><span class="sxs-lookup"><span data-stu-id="69245-357">Create an `if` block that tested to see whether the relevant search box had a value.</span></span>
- <span data-ttu-id="69245-358">`selectCommand`Değişkeni yeni BIR SQL ifadesine ayarlayın.</span><span class="sxs-lookup"><span data-stu-id="69245-358">Set the `selectCommand` variable to a new SQL statement.</span></span>
- <span data-ttu-id="69245-359">`searchTerm`Değişkeni sorguya geçirilecek değere ayarlayın.</span><span class="sxs-lookup"><span data-stu-id="69245-359">Set the `searchTerm` variable to the value to pass to the query.</span></span>

<span data-ttu-id="69245-360">Aşağıda bir başlık araması için yeni mantığı içeren tüm kod bloğu verilmiştir:</span><span class="sxs-lookup"><span data-stu-id="69245-360">Here's the complete code block, which contains the new logic for a title search:</span></span>

[!code-cshtml[Main](form-basics/samples/sample12.cshtml)]

<span data-ttu-id="69245-361">Bu kodun ne işe yönelik bir özet aşağıda verilmiştir:</span><span class="sxs-lookup"><span data-stu-id="69245-361">Here's a summary of what this code does:</span></span>

- <span data-ttu-id="69245-362">Değişkenler `searchTerm` ve `selectCommand` en üstte başlatılır.</span><span class="sxs-lookup"><span data-stu-id="69245-362">The variables `searchTerm` and `selectCommand` are initialized at the top.</span></span> <span data-ttu-id="69245-363">Bu değişkenleri, kullanıcının sayfada ne yaptığını temel alarak uygun arama terimine (varsa) ve uygun SQL komutuna göre ayarlayacağız.</span><span class="sxs-lookup"><span data-stu-id="69245-363">You're going to set these variables to the appropriate search term (if any) and appropriate SQL command based on what the user does in the page.</span></span> <span data-ttu-id="69245-364">Varsayılan arama, tüm filmlerden veritabanından alınması için basit bir durumdur.</span><span class="sxs-lookup"><span data-stu-id="69245-364">The default search is the simple case of getting all the movies from the database.</span></span>
- <span data-ttu-id="69245-365">Ve için testlerinde `searchGenre` `searchTitle` , kod, `searchTerm` aramak istediğiniz değere ayarlanır.</span><span class="sxs-lookup"><span data-stu-id="69245-365">In the tests for `searchGenre` and `searchTitle`, the code sets `searchTerm` to the value you want to search for.</span></span> <span data-ttu-id="69245-366">Bu kod blokları `selectCommand` , bu arama için uygun BIR SQL komutuna de ayarlanır.</span><span class="sxs-lookup"><span data-stu-id="69245-366">Those code blocks also set `selectCommand` to an appropriate SQL command for that search.</span></span>
- <span data-ttu-id="69245-367">`db.Query`Yöntemi yalnızca bir kez çağrılır, HANGI SQL komutunun içinde olduğu `selectedCommand` ve hangi değerin bulunduğu `searchTerm` .</span><span class="sxs-lookup"><span data-stu-id="69245-367">The `db.Query` method is invoked only once, using whatever SQL command is in `selectedCommand` and whatever value is in `searchTerm`.</span></span> <span data-ttu-id="69245-368">Arama terimi (tarz yok ve başlık sözcüğü yoksa) yoksa, değeri `searchTerm` boş bir dizedir.</span><span class="sxs-lookup"><span data-stu-id="69245-368">If there is no search term (no genre and no title word), the value of `searchTerm` is an empty string.</span></span> <span data-ttu-id="69245-369">Ancak, bu nedenle sorgu bir parametre gerektirmediğinden bu durum değildir.</span><span class="sxs-lookup"><span data-stu-id="69245-369">However, that doesn't matter, because in that case the query doesn't require a parameter.</span></span>

## <a name="testing-the-title-search-feature"></a><span data-ttu-id="69245-370">Başlık arama özelliğini test etme</span><span class="sxs-lookup"><span data-stu-id="69245-370">Testing the Title Search Feature</span></span>

<span data-ttu-id="69245-371">Artık tamamlanan arama sayfanızı test edebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="69245-371">Now you can test your completed search page.</span></span> <span data-ttu-id="69245-372">*Filmleri. cshtml*'yi çalıştırın.</span><span class="sxs-lookup"><span data-stu-id="69245-372">Run *Movies.cshtml*.</span></span>

<span data-ttu-id="69245-373">Bir tarz girin ve **Ara türe**tıklayın.</span><span class="sxs-lookup"><span data-stu-id="69245-373">Enter a genre and click **Search Genre**.</span></span> <span data-ttu-id="69245-374">Kılavuz, daha önce olduğu gibi bu tarzın filmlerini görüntüler.</span><span class="sxs-lookup"><span data-stu-id="69245-374">The grid displays movies of that genre, like before.</span></span>

<span data-ttu-id="69245-375">Bir başlık sözcüğü girin ve **başlık ara**' yı tıklatın.</span><span class="sxs-lookup"><span data-stu-id="69245-375">Enter a title word and click **Search Title**.</span></span> <span data-ttu-id="69245-376">Kılavuz, başlığında bu sözcüğe sahip olan filmleri görüntüler.</span><span class="sxs-lookup"><span data-stu-id="69245-376">The grid displays movies that have that word in the title.</span></span>

![Başlıkta ' The ' aranırken filmlere sayfa listeleme](form-basics/_static/image6.png)

<span data-ttu-id="69245-378">Her iki metin kutusunu da boş bırakın ve düğmeye tıklayın.</span><span class="sxs-lookup"><span data-stu-id="69245-378">Leave both text boxes blank and click either button.</span></span> <span data-ttu-id="69245-379">Kılavuzda tüm filmler görüntülenir.</span><span class="sxs-lookup"><span data-stu-id="69245-379">The grid displays all the movies.</span></span>

## <a name="combining-the-queries"></a><span data-ttu-id="69245-380">Sorguları birleştirme</span><span class="sxs-lookup"><span data-stu-id="69245-380">Combining the Queries</span></span>

<span data-ttu-id="69245-381">Gerçekleştirebileceğiniz aramaların dışlamalı olduğunu fark edebilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="69245-381">You might notice that the searches you can perform are exclusive.</span></span> <span data-ttu-id="69245-382">Her iki arama kutusunda de değerler olsa bile başlık ve tarzda aynı anda arama yapamazsınız.</span><span class="sxs-lookup"><span data-stu-id="69245-382">You can't search the title and the genre at the same time, even if both search boxes have values in them.</span></span> <span data-ttu-id="69245-383">Örneğin, başlığı "Adventure" içeren tüm eylem filmlerini araymıyorsunuz.</span><span class="sxs-lookup"><span data-stu-id="69245-383">For example, you can't search for all action movies whose title contains "Adventure".</span></span> <span data-ttu-id="69245-384">(Sayfa şu anda kodlandığında, hem tarz hem de başlık için değer girerseniz, başlık araması önceliklidir.) Koşulları birleştiren bir arama oluşturmak için, aşağıdaki gibi bir sözdizimi içeren bir SQL sorgusu oluşturmanız gerekir:</span><span class="sxs-lookup"><span data-stu-id="69245-384">(As the page is coded now, if you enter values for both genre and title, the title search gets precedence.) To create a search that combines the conditions, you would have to create a SQL query that has syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Genre = @0 AND Title LIKE @1`

<span data-ttu-id="69245-385">Ve aşağıdaki gibi bir ifade kullanarak (kabaca konuşulur) sorguyu çalıştırmanız gerekir:</span><span class="sxs-lookup"><span data-stu-id="69245-385">And you'd have to run the query by using a statement like the following (roughly speaking):</span></span>

`var selectedData = db.Query(selectCommand, searchGenre, searchTitle);`

<span data-ttu-id="69245-386">Birçok permütasyon arama ölçütünden izin vermek için mantık oluşturma, görebileceğiniz gibi bir bit dahil olabilir.</span><span class="sxs-lookup"><span data-stu-id="69245-386">Creating logic to allow many permutations of search criteria can get a bit involved, as you can see.</span></span> <span data-ttu-id="69245-387">Bu nedenle, burada duracağız.</span><span class="sxs-lookup"><span data-stu-id="69245-387">Therefore, we'll stop here.</span></span>

## <a name="coming-up-next"></a><span data-ttu-id="69245-388">Sonraki adımda</span><span class="sxs-lookup"><span data-stu-id="69245-388">Coming Up Next</span></span>

<span data-ttu-id="69245-389">Sonraki öğreticide, kullanıcıların veritabanına film eklemesine izin vermek için form kullanan bir sayfa oluşturacaksınız.</span><span class="sxs-lookup"><span data-stu-id="69245-389">In the next tutorial, you'll create a page that uses a form to let users add movies to the database.</span></span>

## <a name="complete-listing-for-movie-page-updated-with-search"></a><span data-ttu-id="69245-390">Film sayfası listesinin tamamını listeleme (aramayla güncelleştirildi)</span><span class="sxs-lookup"><span data-stu-id="69245-390">Complete Listing for Movie Page (Updated with Search)</span></span>

[!code-cshtml[Main](form-basics/samples/sample13.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="69245-391">Ek Kaynaklar</span><span class="sxs-lookup"><span data-stu-id="69245-391">Additional Resources</span></span>

- [<span data-ttu-id="69245-392">Razor söz dizimini kullanarak ASP.NET Web programlamaya giriş</span><span class="sxs-lookup"><span data-stu-id="69245-392">Introduction to ASP.NET Web Programming Using the Razor Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkID=202890)
- <span data-ttu-id="69245-393">W3Schools sitesinde [SQL WHERE yan tümcesi](http://www.w3schools.com/sql/sql_where.asp)</span><span class="sxs-lookup"><span data-stu-id="69245-393">[SQL WHERE Clause](http://www.w3schools.com/sql/sql_where.asp) on the W3Schools site</span></span>
- <span data-ttu-id="69245-394">W3C sitesindeki [Yöntem tanımları](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) makalesi</span><span class="sxs-lookup"><span data-stu-id="69245-394">[Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="69245-395">[Önceki](displaying-data.md) 
>  [Sonraki](entering-data.md)</span><span class="sxs-lookup"><span data-stu-id="69245-395">[Previous](displaying-data.md)
[Next](entering-data.md)</span></span>
